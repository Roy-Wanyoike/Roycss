{
  "name": "roycss-inspector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "RoyCSS Inspector — Manifest V3 Chrome extension. Inspect any website for RoyCSS classes.",
  "license": "MIT",
  "author": "Royford Wanyoike Wamaitha",
  "scripts": {
    "build:data": "bun run scripts/extract-top-effects.ts",
    "build:ts": "bun build src/background.ts src/content.ts src/popup.ts src/sidepanel.ts src/inspector-overlay.ts src/effects-data.ts --outdir dist/src --target browser",
    "build:assets": "node scripts/copy-assets.mjs",
    "build": "bun run build:data && bun run build:ts && bun run build:assets",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.2.2",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
