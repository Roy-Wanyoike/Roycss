# RoyCSS — Pending Features & Implementation Backlog

> Consolidated from all audit reports. This is the single source of truth
> for pending work. The old report docs will be removed after this list
> is committed.
>
> **Task ID:** PENDING-AUDIT · **Generated by:** Pending-Features Consolidator
> (sub-agent) · **Replaces:** `docs/PENDING-FEATURES-AUDIT.md` (889-line
> cross-reference) and the per-domain `ROYCSS_BACKEND_TODO.md`,
> `ROYCSS_BACKEND_ARCHITECTURE.md`, `ROYCSS_MIGRATION_GUIDE.md`,
> `ROYCSS_DATABASE_ARCHITECTURE.md`, `ROYCSS_API_SPECIFICATION.md`,
> `ROYCSS_SECURITY_REPORT.md`, `ROYCSS_PERFORMANCE_REPORT.md`,
> `ROYCSS_OBSERVABILITY.md`, `ROYCSS_DEPLOYMENT.md`, `ROYCSS_TEST_REPORT.md`,
> `50-ORIGINAL-FEATURES.md`, `PLATFORM-VISION.md`, `ROYCSS-V2-BLUEPRINT.md`,
> `plans/BACKEND-COMPLETION-REQUIREMENTS.md`, and `docs/reports/*.md`.

## How to use this list
Each item has an ID (e.g. PF-001), a title, priority, owner area, current
state, acceptance criteria, likely files to touch, and recommended agent
type. Dispatch agents per item. Update an item's `State` field in place
when you start/finish it; do not duplicate items — extend an existing one
if scope expands.

## Priority legend
- **P0** — go-live blocker (blocks the next production / paid-tier launch;
  the current public marketing/demo site at `/` is already live — see
  "Current state" below)
- **P1** — ship within 1 week of go-live (cleanup + hardening)
- **P2** — v1.1 (compounds the platform, ecosystem expansion)
- **P3** — v2 roadmap (monorepo, compiler, language, vertical products)

## Current state (read before dispatching)
- The **public marketing/demo site at `/` is live** (verified end-to-end by
  worklog Tasks 1–2 and AUDIT-1: 1,749 effects, 62 platform products, 68
  backend-node modules, 13/13 Next.js API routes, 34 Live badges, 0 Demo).
- The three original go-live blockers from AUDIT-1 — **F1** (37 product
  cards discarded `useBackendData`), **F2** (`BackendLiveBadge` dead
  imports), **F8** (sync/tokens 10% failure injection) — are **DONE** per
  worklog FIX-F1-F2 and Task 5. They are NOT re-listed here.
- `backend-node/` (Express + Prisma + SQLite) is the running source of
  truth: 68 modules, 45 Prisma models, `/api/v1`, all modules return real
  JSON envelopes (29 with real DB persistence, 7 reading build artifacts,
  10 real CSS reference implementations, 5 LLM with mock fallback, 3
  Playwright with mock fallback, 4 external-service with mock fallback,
  3 fully real, 6 intentional static snapshots, 4 catalog/scaffold
  mocks, 1 live socket.io).
- `backend-go/` is the production target: 70 .go files, **3 real modules**
  (auth, effects, health) + **66 stub modules returning 501** (honest
  failover design) + complete `pkg/` platform layer + `cmd/{api,migrate,
  worker}` + `Dockerfile` + `docker-compose.yml` + `terraform/main.tf`.
  Per task instructions, the 66 stubs are ONE batched P1 item (PF-008),
  NOT 66 separate items.
- The 96 HIGH-priority NOT-STARTED items from
  `PENDING-FEATURES-AUDIT.md` Tier 1 (V2 vision) are grouped into ~16
  themed P3 items below, not 96 separate dispatches.
- Sources that were **stale / superseded** and noted as such:
  - `docs/plans/BACKEND-COMPLETION-REQUIREMENTS.md` §2.3 says "62 product
    cards don't fetch from backend" — **stale**; 37 cards now use
    `useBackendData` (per AUDIT-1 F1/FIX-F1-F2). Replaced by this file.
  - `docs/plans/BACKEND-COMPLETION-REQUIREMENTS.md` §2.1 says "current
    schema has 4 models" — **stale**; schema now has 45 Prisma models
    (per AUDIT-1). Replaced by this file.
  - `docs/reports/AUDIT-REPORT.md` and `docs/reports/FINAL-VERIFICATION.md`
    claim "all 68 backend modules with real implementations" —
    **overstated**; per AUDIT-1, 6 modules are intentional static
    snapshots (analytics, edge, plugin-hub, mcp, scaffold, refactor) and
    4 are catalog/scaffold mocks. This file is the accurate view.
  - `docs/50-ORIGINAL-FEATURES.md` (744 lines) and `docs/PLATFORM-VISION.md`
    (638 lines) describe strategic ideas; their implementation status is
    cross-referenced in `PENDING-FEATURES-AUDIT.md` Sections 1–2, which
    this file supersedes.

## Summary counts
- **P0: 7**
- **P1: 8**
- **P2: 16**
- **P3: 16**
- **Total: 47**

---

## P0 — Go-live blockers (next production / paid-tier launch)

> The current public marketing/demo site at `/` is live and has **zero**
> remaining P0 blockers (AUDIT-1 F1/F2/F8 are DONE). The 7 P0 items below
> block the next meaningful launch: paid-tier (Roy Cloud, Roy Marketplace
> payments, Roy Pro Components, Roy Enterprise) and any production GA
> where the backend is exposed publicly beyond the read-only marketing
> site.

### PF-001: Apply `requireAuth` + per-org/team role checks to all mutating endpoints
- **Area:** backend-node
- **State:** partial — `requireAuth` middleware exists in
  `backend-node/src/server/middleware/auth.ts` but is only used in
  `auth/routes.ts`. All 67 other modules expose unauthenticated mutating
  endpoints (POST/PUT/DELETE) for favorites, collections, dashboards,
  workspace, marketplace, enterprise, governance, audit-center, billing,
  subscriptions, usage, etc. (AUDIT-1 F3, ROYCSS_BACKEND_TODO A2,
  ROYCSS_SECURITY_REPORT §3, ROYCSS_API_SPECIFICATION §3.)
- **Acceptance:**
  1. Every mutating endpoint (POST/PUT/DELETE) on every non-auth module
     requires a valid JWT (401 when absent).
  2. Per-org/team role checks: OWNER / ADMIN / MEMBER / VIEWER on
     org-scoped resources (`projects`, `studio`, `marketplace`, `cloud`,
     `enterprise`, `governance`, `billing`). Unauthorized role → 403.
  3. `requireRole(role)` middleware composes with `requireAuth`.
  4. Private project is invisible to non-org-members; role-escalation
     blocked (MEMBER cannot DELETE org).
  5. Contract tests cover all of the above (see PF-007).
- **Files:** `backend-node/src/server/middleware/auth.ts` (add
  `requireRole`), `backend-node/src/modules/*/routes.ts` (apply
  middleware on mutating routes), `backend-node/src/modules/{enterprise,organizations,teams}/service.ts`
  (role resolution).
- **Agent:** general-purpose

### PF-002: API key management (CLI / SDK / MCP authentication)
- **Area:** backend-node
- **State:** not started (ROYCSS_BACKEND_TODO A3, ROYCSS_API_SPECIFICATION
  §3, ROYCSS_SECURITY_REPORT §10.)
- **Acceptance:**
  1. `POST /api/v1/auth/api-keys` (Bearer) creates a key — hash at rest
     with bcrypt, plaintext shown ONCE in the response, scoped to
     `scopes` array (e.g. `["effects:read","marketplace:write"]`).
  2. `GET /api/v1/auth/api-keys` (Bearer) lists keys masked
     (`roycss_…XXXX`).
  3. `DELETE /api/v1/auth/api-keys/:id` (Bearer) revokes (sets
     `revokedAt`).
  4. `X-API-Key` auth middleware alternates with `Authorization: Bearer`
     JWT — any protected route accepts either.
  5. Prisma `ApiKey` model with `id`, `hash`, `name`, `scopes`, `ownerId`,
     `orgId?`, `createdAt`, `lastUsedAt`, `revokedAt`.
  6. Per-API-key rate limiting in addition to per-IP (ROYCSS_SECURITY_REPORT §5).
- **Files:** `backend-node/src/modules/auth/{routes,service,schema}.ts`,
  `backend-node/src/server/middleware/auth.ts` (add `apiKeyAuth`),
  `backend-node/prisma/schema.prisma` (add `ApiKey` model).
- **Agent:** general-purpose

### PF-003: Production infrastructure (PostgreSQL + Redis + S3 + workers + LLM/Email/Sentry)
- **Area:** infra
- **State:** not started in sandbox (no Go/PG/Redis toolchain). Artifacts
  present: `database/sql/0*.sql` (14 PG migrations), `infrastructure/docker/docker-compose.yml`
  (postgres:16 + redis:7 + migrate + api + worker), `infrastructure/terraform/main.tf`
  (Cloud SQL PG + Memorystore Redis + Cloud Run api/worker + Secret
  Manager), `backend-go/Dockerfile` (multi-stage). ROYCSS_BACKEND_TODO §B,
  ROYCSS_DEPLOYMENT, ROYCSS_PERFORMANCE_REPORT §3, BACKEND-COMPLETION-REQUIREMENTS §2.5.)
- **Acceptance:**
  1. PostgreSQL 15+ provisioned; all 14 `database/sql/*.sql` migrations
     applied; `psql roycss -c "SELECT count(*) FROM effects;"` returns 1749
     after `999_seed.sql`.
  2. Redis 7+ provisioned; cache + rate-limit + job queue wired through
     it; TTL + invalidation rules per ROYCSS_DATABASE_ARCHITECTURE §4.
  3. S3-compatible object storage provisioned for `storage` module and
     marketplace asset uploads.
  4. Worker queue running — either BullMQ + Redis in `backend-node` or
     the Go `cmd/worker` binary in `backend-go`; jobs persist across API
     restarts.
  5. At least one LLM API key (`OPENAI_API_KEY` or `ANTHROPIC_API_KEY`)
     set; 5 LLM modules switch from mock to real provider.
  6. `RESEND_API_KEY` set; contact form sends transactional email.
  7. `SENTRY_DSN` set; error tracking flowing.
  8. `GET /api/v1/health/ready` returns 200 with `checks:{db,redis,registry,search}`
     all `connected`; 503 if any critical dependency is down (ROYCSS_OBSERVABILITY §5).
- **Files:** `infrastructure/docker/docker-compose.yml`,
  `infrastructure/terraform/main.tf`, `backend-node/.env`,
  `backend-node/src/config/env.ts` (add PG/Redis validation),
  `backend-node/src/lib/{cache,queue}.ts` (Redis adapters),
  `backend-node/src/server/app.ts` (mount `/health/ready`).
- **Agent:** general-purpose (DevOps)

### PF-004: Third-party WCAG 2.2 AA audit + VPAT 2.4 + per-effect a11y tags
- **Area:** docs / a11y / frontend
- **State:** partial — `tests/a11y/{axe-audit,keyboard-nav,visual-checks}.ts`
  run build-time audits; no third-party audit, no VPAT, no per-effect
  a11y tag in catalog UI. (ENTERPRISE-REVIEW R2/R3 Critical, LABS-30 §7.10,
  V2 §16.10, AUDIT-REPORT §11.)
- **Acceptance:**
  1. `docs/ACCESSIBILITY-AUDIT.md` published — third-party WCAG 2.2 AA
     audit report signed by an external auditor (scope: live
     `roycss.dev`, all 1,749 effects, 62 product cards, docs site).
  2. `docs/VPAT-2.4.md` published — VPAT 2.4 EU edition, fully populated
     against the live site (per-criterion: Supports / Partially Supports
     / Does Not Support / Not Applicable, with remarks).
  3. `src/lib/effect-taxonomy.ts` extended with `a11y` field per effect:
     `{ motionSafe: boolean, decorationOnly: boolean, requiresAria?: string[] }`.
  4. `src/components/roycss/effect-card.tsx` displays the a11y tag
     (motion-safe / decoration-only) so users can filter.
  5. Effects allowlist published in `docs/EFFECT-A11Y-TIERS.md`
     (text-safe vs. decoration-only).
- **Files:** `docs/ACCESSIBILITY-AUDIT.md` (new), `docs/VPAT-2.4.md` (new),
  `docs/EFFECT-A11Y-TIERS.md` (new), `src/lib/effect-taxonomy.ts`,
  `src/components/roycss/effect-card.tsx`.
- **Agent:** general-purpose (coordinate with external WCAG auditor)

### PF-005: LTS + SLA + Governance docs (bus-factor + enterprise readiness)
- **Area:** docs
- **State:** not started. ENTERPRISE-REVIEW §16.1 R1 (bus factor —
  Critical), R3 (no LTS/SLA — Critical). V2 §16.6/16.12/16.15/16.16/16.17/16.18.
  LABS-30 §7.3. COMPETITIVE-ANALYSIS R10/R15.
- **Acceptance:**
  1. `docs/LTS.md` — one major LTS at all times, 18 months after
     successor ships, critical patches backported, breaking changes
     announced one minor ahead with codemods.
  2. `docs/SLA.md` — 1h P0 / 4h P1 / NBD P2 response times, dedicated
     Slack channel, quarterly architecture review.
  3. `docs/SECURITY-SLA.md` — 72h critical-fix SLA, public mailbox
     (`security@roycss.dev`), PGP key published, GitHub Security
     Advisories enabled.
  4. `MAINTAINERS.md` — named co-maintainers per area (frontend,
     backend-node, backend-go, infra, docs, a11y, security) — bus-factor
     mitigation.
  5. `docs/GOVERNANCE.md` — 5 core maintainers + 9-member steering
     committee charter, quarterly elections, decision-making process.
  6. `CODE_OF_CONDUCT.md` — Contributor Covenant 2.1, 3-person
     moderation team.
  7. `SEMVER.md` + `DEPRECATION.md` — SemVer policy, deprecation
     timeline (1 minor warning → codemod → 6 months → removal in next
     major).
  8. `rfcs/` directory created with `0000-template.md` + first example
     RFC; `docs/RFC-PROCESS.md` documents 14-day review, 2 core + 3
     community approvals.
- **Files:** `docs/{LTS,SLA,SECURITY-SLA,GOVERNANCE,DEPRECATION,SEMVER,RFC-PROCESS}.md`
  (new), `MAINTAINERS.md`, `CODE_OF_CONDUCT.md`, `rfcs/` (new dir).
- **Agent:** general-purpose

### PF-006: CI performance + bundle-size gate (Lighthouse CI + size-limit + budget.json)
- **Area:** infra / CI
- **State:** partial — `perf/regression.test.ts` + `perf/benchmarks/*`
  measure but no CI gate; no Lighthouse CI; no size-limit; no budget.json.
  (LABS-33 §9, V2 §11.5, ENTERPRISE-REVIEW R5, LABS-30 §7.7.)
- **Acceptance:**
  1. `perf/budget.json` committed with 12 metrics (CSS gzip ≤ 28KB, raw
     ≤ 280KB, DOM ≤ 8000, animations ≤ 60 active, `backdrop-filter` ≤ 50,
     LCP ≤ 1.5s, INP ≤ 50ms, CLS < 0.1, TBT < 200ms).
  2. `.github/workflows/ci.yml` budget gate fails the build when any
     metric exceeds budget.
  3. `lighthouserc.json` committed; Lighthouse CI runs on every PR
     against a preview build; scores posted as a PR comment.
  4. `.size-limit.json` committed; `size-limit` runs on every PR; the
     CSS + JS bundle delta is reported in the PR check.
  5. Coverage gate: ≥80% statements, ≥70% branches (also required by
     PF-007).
- **Files:** `.github/workflows/ci.yml`, `lighthouserc.json` (new),
  `.size-limit.json` (new), `perf/budget.json` (new),
  `package.json` (add `size-limit`, `@lhci/cli` deps).
- **Agent:** general-purpose

### PF-007: Backend integration + contract test coverage (all 68 modules)
- **Area:** backend-node / tests
- **State:** partial — only `auth`, `effects`, `contact` have integration
  tests (15 total). 65 modules have zero integration tests; no contract
  test harness. (ROYCSS_BACKEND_TODO A8, ROYCSS_TEST_REPORT §5,
  ROYCSS_V2-BLUEPRINT §13.)
- **Acceptance:**
  1. At least one integration test per `backend-node/src/modules/*`
     module (68 total) using `supertest` against a test SQLite DB.
  2. Contract test harness in `backend-node/tests/contract/` pinning the
     `/api/v1` response shape for every `GET list`, `GET single`,
     `POST`, `PATCH`, `DELETE` — these become the acceptance tests for
     the `backend-go` port (PF-008).
  3. Security tests: authn, authz (per PF-001), injection (SQL/XSS),
     rate-limit, secret-exposure. Run in CI.
  4. Load test plan in `tests/load/` (k6) for hot endpoints:
     `/health/live`, `GET /effects`, `GET /effects/:slug`, `GET /search`,
     `POST /auth/login` — 100 / 1,000 / 10,000 concurrent requests,
     record p50/p95/p99 + error rate.
  5. E2E (Playwright) wired into CI (currently manual via Agent Browser).
  6. Coverage ≥80% statements / ≥70% branches, enforced.
- **Files:** `backend-node/tests/integration/*.test.ts` (new per
  module), `backend-node/tests/contract/*.test.ts` (new),
  `backend-node/tests/security/*.test.ts` (new),
  `tests/load/*.k6.js` (extend existing),
  `.github/workflows/ci.yml` (add jobs).
- **Agent:** general-purpose

---

## P1 — Ship within 1 week of go-live (cleanup + hardening)

### PF-008: backend-go batched production port (66 stub modules → real, single batch)
- **Area:** backend-go
- **State:** 3 of 68 modules real (`internal/auth`, `internal/effects`,
  `internal/health`); 66 stub modules return 501 with a stable JSON
  envelope (honest failover — frontend's `useBackendData` gets 501 →
  falls back to demo data → shows "Demo" badge). `pkg/` platform layer
  complete (`config`, `logger`, `database`, `cache`, `auth`, `http`,
  `response`, `storage`, `validator`, `redis`); `cmd/{api,migrate,worker}`
  wired; `Dockerfile` + `docker-compose.yml` + `terraform/main.tf` +
  `api/openapi/openapi.yaml` present. **Cannot compile in sandbox — no
  Go toolchain.** (ROYCSS_BACKEND_TODO §B, ROYCSS_MIGRATION_GUIDE §5,
  worklog Task 5.)
- **Acceptance (single batched engagement; `ROYCSS_MIGRATION_GUIDE.md` is the playbook):**
  1. Port the 66 stub modules to real Go implementations following the
     migration guide order: Phase 2 (users, organizations, teams) →
     Phase 3 (registry: effects/components/patterns/collections/recipes/
     themes/tokens/icons/motion) → Phase 4 (projects, playground, studio,
     marketplace, ai, mcp, cli, inspector, devtools, accessibility,
     analytics, cloud, billing, subscriptions, usage, search,
     notifications, audit) → Phase 5 (remaining 28 modules).
  2. Per-module Go shape: `handler.go` (HTTP only) + `service.go` +
     `repository.go` + `models.go` + `dto.go` + `routes.go` +
     `<module>_test.go`. Dependency direction enforced:
     `handler → service → domain → repository → pgx` (never
     `handler → pgx` / `handler → redis` / `handler → AI provider`).
  3. Workers extracted into `cmd/worker/main.go` — Redis-backed job
     queue, workers for ai/accessibility/analytics/search/marketplace/
     notifications/exports/screenshots/asset-processing. HTTP handlers
     enqueue (return `{ jobId }`) and workers dequeue.
  4. OpenAPI generated from the Go routes (replaces PF-009's TS
     generator when the cutover is complete).
  5. Per-module cutover is zero-downtime: run Go alongside TS, feature-
     flag the route, compare responses (contract test from PF-007),
     flip 100%, watch error rate for 24h, then remove the TS module's
     routes (keep code for one release as rollback).
  6. Definition-of-done: all 68 modules serving `/api/v1` from Go;
     PostgreSQL authoritative (SQLite retired); Redis handles cache +
     rate-limit + job queue; the live `/` page renders identically
     (same title, body length, Live-badge count); no data lost (row
     counts match before/after); no existing feature/route/effect/
     component removed.
- **Files:** `backend-go/internal/*/handler.go` (66 files upgraded from
  501 stub to real impl); new `service.go`, `repository.go`, `models.go`,
  `dto.go`, `routes.go`, `<module>_test.go` per module; `backend-go/cmd/worker/main.go`
  (extend existing).
- **Agent:** Plan (single batched engagement — needs a Go 1.23+ host
  outside this sandbox; PF-007 contract tests are the acceptance
  criteria). Dispatch as ONE ticket, not 66.

### PF-009: backend-node hardening batch (TODO A1, A4, A5, A6, A7, A9)
- **Area:** backend-node
- **State:** partial — registry module is npm-backed; OpenAPI is inline
  in `routes.ts` files; rate-limit is in-memory sliding window; audit
  module exists but coverage is sparse; workers are in-process;
  observability has structured logs but no per-route metrics. (ROYCSS_BACKEND_TODO
  A1/A4/A5/A6/A7/A9, ROYCSS_OBSERVABILITY §7, ROYCSS_API_SPECIFICATION §2.)
- **Acceptance:**
  1. **A1 — Registry single source of truth:** `GET /api/v1/registry/resolve/:slug`
     returns the canonical effect/component/pattern regardless of
     caller. `effects`, `components`, `patterns`, `themes`, `tokens`,
     `icons`, `motion` modules delegate reads to the registry module
     (one code path for RoyCSS framework content). Version stamping:
     every registry item exposes `version` + `latestVersion`;
     `GET /api/v1/registry/packages/:id/versions` returns a diff.
  2. **A4 — OpenAPI generation:** generator walks each module's
     `routes.ts` + Zod `schema.ts` → emits `api/openapi.yaml`. Serve
     `GET /api/v1/openapi.json` + Swagger UI at `/api/docs`.
  3. **A5 — Rate-limit tiers behind an interface:** in-memory sliding
     window moved behind a `RateLimiter` interface so it can swap to
     Redis in PF-003 / PF-008 without changing call sites. Per-route
     tiers: `general` / `auth` / `contact` / `ai` / `search`.
  4. **A6 — Audit log coverage:** every mutating endpoint (auth,
     billing, marketplace, enterprise, governance, audit-center,
     favorites, collections, workspace, settings) writes to the
     `audit` module / `EnterpriseAuditLog` table. `GET /api/v1/audit?actor=&action=&since=`
     query endpoint for admins.
  5. **A7 — Worker abstraction (in-process first):** extract
     accessibility audits, AI generations, analytics aggregation into a
     job-queue abstraction (in-process today; Redis-backed in PF-003 /
     PF-008). `POST /api/v1/<module>/jobs` → `{ jobId }`;
     `GET .../jobs/:id` → `{ status, result }`.
  6. **A9 — Observability:** `/api/v1/health/ready` checks DB + registry
     + search index (Redis check added in PF-003). Per-route latency
     histograms (in-memory p50/p95/p99). `requestId` propagated
     end-to-end (frontend → backend, already in `X-Request-Id` response
     header; add to frontend → backend call).
- **Files:** `backend-node/src/modules/registry/{routes,service}.ts`,
  `backend-node/src/modules/{effects,components,patterns,themes,tokens,icons,motion}/service.ts`,
  `backend-node/scripts/gen-openapi.ts` (new), `backend-node/src/server/middleware/rateLimit.ts`
  (interface), `backend-node/src/modules/audit/{routes,service}.ts`,
  `backend-node/src/lib/queue.ts` (new), `backend-node/src/server/middleware/logging.ts`
  (histograms), `backend-node/src/server/app.ts` (mount `/health/ready`,
  `/api/docs`).
- **Agent:** general-purpose

### PF-010: Marketplace + sponsorship payment wiring (Stripe Connect, idempotent purchases, creator payouts)
- **Area:** backend-node / frontend
- **State:** partial — `marketplace.tsx` UI exists; `backend-node/src/modules/marketplace/`
  exists with Prisma `Template`, `TemplateReview` models; no real
  creator onboarding, no Stripe Connect, no payouts, no item-review
  pipeline. `pricing-section.tsx` sponsorship modal says "Stripe (coming
  soon)". (PLATFORM-VISION §1.5, ROYCSS_API_SPECIFICATION §7, R12 partial.)
- **Acceptance:**
  1. Stripe Connect Express accounts for creators; KYC handled by Stripe.
  2. `POST /api/v1/marketplace/purchases` is idempotent (Stripe-style
     idempotency key in header; double-submit → one charge).
  3. `POST /api/v1/marketplace/refunds` idempotent; payout reversals
     prorated.
  4. Creator payout flow: 85% to creator, 15% platform commission,
     weekly payout schedule.
  5. Item-review pipeline: upload validation (no in-process execution
     of untrusted code — ROYCSS_SECURITY_REPORT §1); sandboxed preview;
     WCAG 2.1 AA + RoyCSS-namespace + framework-agnostic + <50KB gzip
     per item; reviewer approval before listing.
  6. Sponsorship modal: real Stripe Checkout (currently "coming soon").
  7. Roy Cloud, Roy Pro Components, Roy Enterprise billing tiers wired
     to the same Stripe account (ROYCSS_BACKEND_TODO §B9, ROYCSS_API_SPECIFICATION §10).
- **Files:** `backend-node/src/modules/{marketplace,billing,subscriptions,usage}/{routes,service}.ts`,
  `backend-node/src/lib/stripe.ts` (new), `backend-node/prisma/schema.prisma`
  (Stripe account + payout models), `src/components/roycss/pricing-section.tsx`
  (wire real Stripe Checkout).
- **Agent:** general-purpose

### PF-011: Email service (Resend) + contact form transactional email + notifications
- **Area:** backend-node
- **State:** contact form persists to DB but does not send email;
  `notifications` module exists but no email transport; `RESEND_API_KEY`
  not configured. (ROYCSS_BACKEND_TODO §B, AUDIT-REPORT §11,
  API_KEYS_REQUIRED row 7.)
- **Acceptance:**
  1. `RESEND_API_KEY` set; `backend-node/src/lib/email.ts` wraps the
     Resend SDK with templates (contact-auto-reply, contact-team-
     notification, weekly-digest, billing-receipt, certification-granted).
  2. Contact form: user gets an auto-reply, team gets a notification
     (both via Resend); DB row remains the audit trail.
  3. `notifications` module: `POST /api/v1/notifications/:id/read` (Bearer),
     email + in-app channels, per-user preferences
     (`PATCH /api/v1/users/me` notification settings).
  4. Transactional templates version-controlled in
     `backend-node/src/templates/*.tsx` (React Email) or `.mjml`.
  5. Bounce/complaint webhooks from Resend → update `User.emailBouncedAt`.
- **Files:** `backend-node/src/lib/email.ts` (new),
  `backend-node/src/templates/*.tsx` (new),
  `backend-node/src/modules/{contact,notifications}/service.ts`,
  `backend-node/src/config/env.ts` (validate `RESEND_API_KEY`).
- **Agent:** general-purpose

### PF-012: Convert documented mock limitations to real impl OR label as "demo" in UI
- **Area:** backend-node / frontend
- **State:** AUDIT-1 F4/F5/F6/F7/F9–F12/F14/F15/F16 — six intentional
  static-snapshot modules (analytics, edge, plugin-hub, mcp, scaffold,
  refactor) and four catalog/scaffold mocks. Each module has a
  `Future:` comment. The marketing site shows them as "Live" badges
  even though they return mock data.
- **Acceptance:**
  1. For each module below, EITHER implement real functionality OR add
     an explicit "Demo" UI label (so users are not misled):
     - `analytics` (F4): wire `apiCalls` from a request-log table OR
       label the dashboard "Demo data".
     - `mcp` (F5): spawn `mcp-server/index.ts` as a child process and
       proxy tool calls, OR label "Catalog only — MCP server not
       invoked".
     - `edge` (F6): real edge-platform API OR label "Demo regions".
     - `plugin-hub` (F7): real npm `roycss-plugin-*` discovery OR label
       "Curated catalog".
     - `live` (F9): persist chat messages to `LiveMessage` (Prisma model
       already exists) OR document in-memory.
     - `accessibility` (F10): require Playwright + Chromium in
       production deploys (per ROYCSS_DEPLOYMENT §5).
     - `digital-twin` (F11): require Lighthouse binary in production
       deploys.
     - `devtools` (F12): require Playwright in production deploys.
     - `refactor` (F14): real CSS-lint AST backend OR document mock
       variance.
     - `scaffold` + `generator` (F15): real Hygen/Plop/codemod OR
       document as template-only.
     - `src/app/api/health/route.ts` (F16): source `effectsCount` from
       `effects.length` or `/api/effects/manifest` count, not the
       hardcoded `1749`.
  2. The 37 product cards using `useBackendData` (per FIX-F1-F2) —
     their `BackendLiveBadge` already shows "Live/Sync/Demo" honestly;
     ensure the labels match the new "Demo" labels above.
- **Files:** `backend-node/src/modules/{analytics,mcp,edge,plugin-hub,live,accessibility,digital-twin,devtools,refactor,scaffold,generator}/service.ts`,
  `src/app/api/health/route.ts`, `src/components/roycss/_backend-live-badge.tsx`.
- **Agent:** general-purpose

### PF-013: Public API surface in `API.md` + CI gate
- **Area:** docs / CI
- **State:** not started. ROYCSS_API_SPECIFICATION is the inline
  reference but no generated `API.md` and no CI gate that prevents
  removing a `stable` name without a major bump. (LABS-30 §7.1, LABS-35
  §2, FIRST-PRINCIPLES-REDESIGN F1.)
- **Acceptance:**
  1. `API.md` at repo root — every class / property / keyframe / token
     / API route, marked `stable` / `experimental` / `deprecated`.
  2. `.github/workflows/api-gate.yml` — fails the build if a `stable`
     name is removed or renamed without a major-version bump in
     `package.json`.
  3. Generated from PF-009's OpenAPI spec + the `dist/` artifacts.
- **Files:** `API.md` (new), `.github/workflows/api-gate.yml` (new),
  `scripts/gen-api-md.ts` (new).
- **Agent:** general-purpose

### PF-014: Per-effect static page (`/effects/<id>`) + versioned docs + tested docs samples
- **Area:** frontend / docs
- **State:** partial. Effects live in the unified single-page catalog
  at `/`, not at `/effects/<id>`. Docs site is single-version. Docs
  code samples are not tested in CI. (V2 §8, LABS-30 §1.3/§7.5,
  COMPETITIVE-ANALYSIS R1, LABS-29 §9.2.)
- **Acceptance:**
  1. `src/app/effects/[id]/page.tsx` — per-effect static page with rich
     metadata (OG image, structured data, "copy CSS" button, framework
     tabs, related effects, a11y tag from PF-004).
  2. `next.config.ts` `output: export` for the per-effect pages
     (statically generated at build time); sitemap auto-includes them.
  3. Versioned docs routing: `src/app/docs/[version]/...` with the
     current version redirecting to the latest; per-minor snapshots.
  4. Docs "Edit on GitHub" link per page (LABS-29 §9.2, LABS-30 §8.6).
  5. Per-page feedback widget + last-updated timestamp (LABS-30 §8.7/§8.8).
  6. Code samples tested in CI — a workflow extracts code blocks from
     each docs page and asserts they parse + typecheck + render.
- **Files:** `src/app/effects/[id]/page.tsx` (new),
  `next.config.ts` (`output: export`), `src/app/docs/[version]/...` (new
  routing), `src/components/docs/{edit-link,feedback,timestamp}.tsx` (new),
  `.github/workflows/docs-test.yml` (new).
- **Agent:** general-purpose

### PF-015: Codemod library — inbound (Tailwind / Bootstrap / Animate.css / MUI / Chakra → RoyCSS) + outbound + V1→V2
- **Area:** tooling
- **State:** partial. `scripts/migrate-colors.ts` and
  `scripts/migrate-logical.ts` exist for self only. No V1→V2 codemod,
  no inbound from competitors, no outbound to vanilla CSS. (V2 §14,
  COMPETITIVE-ANALYSIS R3, LABS-34 §5.5, LABS-35 §9, ENTERPRISE-REVIEW R11.)
- **Acceptance:**
  1. `scripts/codemods/v1-to-v2.ts` — V1 → V2 (depends on V2 packages
     existing — schedule after PF-042 starts).
  2. `scripts/codemods/from-tailwind.ts` — Tailwind classes → RoyCSS
     classes (semantic, not 1:1 swap; uses design tokens).
  3. `scripts/codemods/from-bootstrap.ts` — Bootstrap → RoyCSS.
  4. `scripts/codemods/from-animate-css.ts` — Animate.css → RoyMotion.
  5. `scripts/codemods/from-mui.ts` — Material UI → RoyCSS.
  6. `scripts/codemods/from-chakra.ts` — Chakra UI → RoyCSS.
  7. `scripts/codemods/to-{tailwind,bootstrap,vanilla-css}.ts` —
     outbound (lock-in prevention per LABS-34 §5.5).
  8. `roycss migrate from-*` and `roycss migrate to-*` CLI commands
     (CLI lives in `cli/index.js`; the codemods are jscodeshift or
     ts-morph transforms).
  9. Per codemod: a fixture corpus + golden-output tests in
     `scripts/codemods/__tests__/`.
- **Files:** `scripts/codemods/*.ts` (new), `cli/index.js` (add
  `migrate` subcommands), `scripts/codemods/__tests__/*` (new).
- **Agent:** general-purpose

---

## P2 — v1.1 (compounds the platform, ecosystem expansion)

> Items 16–31 below map to `PENDING-FEATURES-AUDIT.md` Tier 2 MEDIUM
> (numbered 26–40 in the audit). These are ecosystem products that
> compound the platform's value; many depend on V2 monorepo packages
> (PF-042) for the cleanest implementation, but each can ship as an
> incremental addition to the V1 codebase.

### PF-016: First-party build plugins (Vite, Next.js, Astro, webpack, Turbopack, esbuild, Rollup, Rspack)
- **Area:** tooling
- **State:** not started — manual `@import "roycss.css"` only. (R6.)
- **Acceptance:** Each plugin published as `@roycss/<bundler>-plugin`,
  zero-config, scans consumer source for `r-*` / `roycss-*` class usage,
  extracts only used CSS, supports HMR + JIT in dev + AOT in prod,
  supports critical-CSS streaming for SSR frameworks. Docs page per
  plugin with installation + troubleshooting.
- **Files:** `packages/@roycss/{vite,next,astro,webpack,turbopack,esbuild,rollup,rspack}-plugin/`
  (new, requires PF-042 monorepo) OR `scripts/bundler-plugins/*` (V1
  stopgap).
- **Agent:** general-purpose

### PF-017: RoyMotion gesture library + View Transitions router adapters + cross-document MPA
- **Area:** motion
- **State:** partial — `motion-primitives.tsx`, `motion-library.tsx`
  (60 presets), `animation-timeline.tsx`, `view-transition.tsx` tool
  exist; no gesture library, no router adapters, no MPA cross-document
  support. (V2 §7.6-7.9, F6/F7, Tier 2 #27.)
- **Acceptance:**
  1. `@roycss/motion/gestures/` — drag, swipe, pinch, rotate, tap,
     long-press hooks (20 motion intents with physics, `useDragIntent()`,
     mandatory `prefers-reduced-motion` variants).
  2. `@roycss/motion/view-transitions/` — router adapters for Next.js,
     Astro, SvelteKit, Remix, Nuxt; `vt-name` attribute support.
  3. Cross-document MPA View Transitions support (same-origin).
  4. Spring physics + `linear()` easing named presets library.
  5. 50 choreographed sequences + 30 spring presets + 20 page
     transitions + motion tokens (PLATFORM-VISION §1.12 Premium).
- **Files:** `packages/@roycss/motion/{gestures,view-transitions,physics,sequences,springs,page-transitions,tokens}/`
  (requires PF-042), `src/app/roymotion.css` (extend).
- **Agent:** general-purpose

### PF-018: Multi-surface token emission + bidirectional Figma plugin
- **Area:** tokens / tooling
- **State:** partial — `src/lib/design-tokens.ts` exports JSON only. No
  iOS/Android/Windows/Flutter emission, no Figma plugin. (F13, R9,
  V2 §16.7, Tier 2 #28.)
- **Acceptance:**
  1. `scripts/emit-tokens-{ios.swift,android.kt,figma.json,windows.xaml,flutter.dart}.ts`
     — W3C DTCG JSON → each surface's native format.
  2. New repo `figma-plugin/` — bidirectional OKLCH token sync (Figma
     → Roy Cloud → code; code → Roy Cloud → Figma).
  3. `@roycss/tokenstudio` package (V2) wraps the emission + sync.
  4. CI test: emission round-trips losslessly.
- **Files:** `scripts/emit-tokens-*.ts` (new), `figma-plugin/` (new
  repo), `src/lib/design-tokens.ts` (W3C DTCG JSON output).
- **Agent:** general-purpose

### PF-019: Roy Cloud collaboration (live multi-cursor token editing, Git-backed token repos, audit log, SSO)
- **Area:** backend-node / frontend / infra
- **State:** partial — `backend-node/src/modules/cloud/` module exists;
  no Git-backed token repos, no multi-cursor editing, no CDN theme
  hosting, no SSO, no audit log in cloud module. (PLATFORM-VISION §1.4,
  V2 §16.9, Tier 2 #19/35, COMPETITIVE-ANALYSIS R1.)
- **Acceptance:**
  1. Git-backed W3C DTCG token collections with semver, branch-per-team,
     merge with token-level conflict resolution.
  2. Live multi-cursor editing (Figma-style) via the existing
     `mini-services/live-service/` (extend to token files).
  3. CDN theme hosting: `<link rel="stylesheet"
     href="https://cdn.roy.cloud/themes/{org}/{theme}@{semver}.css">`.
  4. SSO (Google / GitHub / SAML for Enterprise tier).
  5. Audit log in `cloud/audit` — who changed which token when, with
     diff; exportable for SOC 2 / ISO 27001.
  6. Free tier (1 project, 3 themes, public-only), paid Team/Business/
     Enterprise tiers wired to Stripe (PF-010).
- **Files:** `backend-node/src/modules/cloud/{routes,service}.ts`,
  `mini-services/live-service/index.ts` (extend),
  `backend-node/src/modules/auth/{routes,service}.ts` (add OAuth/SAML),
  `infrastructure/terraform/main.tf` (Cloud CDN + Git backend).
- **Agent:** general-purpose

### PF-020: Roy Marketplace creator onboarding + payouts + item-review pipeline
- **Area:** backend-node / infra
- **State:** partial — UI + backend module exist; no Stripe Connect,
  no creator onboarding, no item-review pipeline. (PLATFORM-VISION
  §1.5, ROYCSS_BACKEND_TODO §B8, Tier 2 #20.) Note: payment plumbing
  shared with PF-010; this item is the creator-side surface.
- **Acceptance:**
  1. Stripe Connect Express onboarding flow for creators (KYC via Stripe).
  2. Creator dashboard: list items, see sales, request payout.
  3. Item-review pipeline (per PF-010 acceptance #5): upload
     validation, sandboxed preview, reviewer approval before listing.
  4. RoyCSS takes 15% commission; weekly payout schedule; payout
     reversals prorated for refunds.
  5. 5 first-party theme packs (Healthcare / SaaS / Fintech /
     Education / E-commerce) commissioned for launch (PLATFORM-VISION
     §6.2).
  6. Marketplace listing page at `/marketplace` (currently in-app
     `marketplace.tsx` only).
- **Files:** `backend-node/src/modules/{marketplace,billing}/service.ts`,
  `src/app/marketplace/page.tsx` (new), `backend-node/src/lib/stripe.ts`.
- **Agent:** general-purpose

### PF-021: Roy AI hybrid retrieval + `@roycss/ai` package + `roycss.rules.md` + training pairs + AI conformance suite
- **Area:** AI / backend-node
- **State:** partial — `roy-ai.tsx` UI + `src/app/api/{ai-migration,ai-playground,css-doctor}/route.ts`
  + 5 LLM backend modules (`architect`, `designer`, `mentor`, `pair`,
  `review`) with mock fallback. No vector index of effects, no hybrid
  retrieval, no fine-tuned RoyCSS model, no `roycss.rules.md`, no
  `@roycss/ai` package, no AI conformance suite. (PLATFORM-VISION §1.6,
  V2 §8.3, LABS-32 §7-9, F11, Tier 2 #17.)
- **Acceptance:**
  1. Vector index of every effect's CSS + description + tags + visual
     screenshot; cosine similarity + structured token lookup + LLM
     reranking for top 10.
  2. `@roycss/ai` package — prompt → CSS/component codegen with
     guaranteed RoyCSS-class vocabulary (no hallucinated classes).
  3. `dist/roycss.rules.md` — RoyCSS system prompt for LLMs.
  4. `dist/roycss.training-pairs.json` — canonical prompt → output
     pairs.
  5. `dist/roycss.system-prompt.md` + `dist/roycss.grammar.json` —
     autocomplete grammar + TypeScript types for AI autocomplete.
  6. `tests/ai-conformance/` — test prompts + scoring rubric + public
     leaderboard (LABS-32 §9).
  7. Roy AI prompt-to-component pipeline (PLATFORM-VISION §4.4):
     `roy-architect.tsx` extension with versioned output.
- **Files:** `backend-node/src/modules/{architect,designer,mentor,pair,review}/service.ts`,
  `backend-node/src/lib/{vector-index,llm-client}.ts` (extend),
  `packages/@roycss/ai/` (requires PF-042), `dist/roycss.{rules.md,training-pairs.json,system-prompt.md,grammar.json}`
  (new), `tests/ai-conformance/` (new).
- **Agent:** general-purpose

### PF-022: Component Genome (per-component manifest with composition + WCAG + browser-support + bundle size)
- **Area:** tooling / data
- **State:** partial — `dist/pro-components.json` and
  `dist/class-index.json` exist but are not per-component Genomes with
  composition graph + WCAG + browser-support + bundle size. (PLATFORM-VISION
  §4.3, Tier 2 #21.)
- **Acceptance:**
  1. `scripts/gen-genome.ts` walks every RoyCSS component (free / Pro /
     Marketplace) and emits `dist/genome/<component>.json`.
  2. Each Genome: `classes[]`, `tokens[]`, `motion[]`, `frameworks[]`,
     `bundleSizeBytes`, `wcagLevel`, `browserSupport{}`, `dependencies[]`.
  3. CLI: `roycss genome <component>` prints the Genome.
  4. Bundle optimization: `roycss build --used <file>` traverses Genomes
     to determine the minimum CSS to ship.
  5. Impact analysis: `roycss impact --token <name>` returns which
     components break in <50ms.
  6. Marketplace search uses Genomes for structured queries ("find me
     a date picker that uses `.roycss-glass-tinted-depth` and is WCAG
     AAA").
- **Files:** `scripts/gen-genome.ts` (new), `dist/genome/*.json` (new),
  `cli/index.js` (add `genome` + `impact` commands).
- **Agent:** general-purpose

### PF-023: Roy Accessibility Suite RUM SDK + AI auto-fix PR opener + `@roycss/a11y` build-fail
- **Area:** a11y / tooling
- **State:** partial — `accessibility-suite.tsx`, `tests/a11y/`,
  `a11y/audit.ts` exist for build-time; no RUM SDK, no AI auto-fix PR
  opener, no `@roycss/a11y` package, no `roycss a11y` CLI, no build-fail
  by default. (V2 §9, F8, PLATFORM-VISION §1.13, Tier 2 #33.)
- **Acceptance:**
  1. `packages/@roycss/a11y/` — axe-core fork with 27 RoyCSS-specific
     rules, packaged as a CLI + library.
  2. `roycss a11y` CLI — fails the build if any page violates the
     configured WCAG level (AA default, AAA optional).
  3. `.github/workflows/ci.yml` gate runs `roycss a11y` — build fails on
     violation.
  4. `packages/@roycss/rum/` — RUM SDK reports real-user accessibility
     violations back to Roy Cloud for triage.
  5. AI auto-fix: `roycss a11y --fix` applies concrete fixes; one-click
     "open PR" in Roy Cloud UI uses Roy AI (PF-021) to suggest fixes.
  6. Global `prefers-reduced-motion` policy published in
     `docs/ACCESSIBILITY-POLICY.md`.
- **Files:** `packages/@roycss/{a11y,rum}/` (requires PF-042),
  `cli/index.js` (add `a11y` command), `docs/ACCESSIBILITY-POLICY.md`
  (new), `.github/workflows/ci.yml` (gate).
- **Agent:** general-purpose

### PF-024: Roy Academy proctored exams + certification issuance pipeline
- **Area:** backend-node / frontend
- **State:** partial — `academy.tsx` UI + `backend-node/src/modules/{academy,certifications,mentor}/`
  exist; no proctored exams, no cert issuance pipeline. (PLATFORM-VISION
  §1.8, Tier 2 #34.)
- **Acceptance:**
  1. 6 courses (101, 201, 301, 401, Studio Mastery, Enterprise) with
     video + written content + exercises.
  2. 4-tier certification (Associate / Professional / Expert /
     Architect) with proctored exams (third-party proctor: ProctorU /
     Examity).
  3. Certification issuance pipeline: exam pass → unique verifiable
     credential (signed JWT or W3C Verifiable Credential) → public
     registry at `roycss.dev/certifications/<id>`.
  4. Free 8-week university curriculum at `/teach` (versioned URLs
     `/docs/2.x/teach`) per LABS-30 §3.1.
  5. Partner with 3 bootcamps + 5 universities for the first year.
- **Files:** `backend-node/src/modules/{academy,certifications}/service.ts`
  (extend), `src/app/teach/page.tsx` (new), `src/app/academy/page.tsx`
  (new), `backend-node/src/lib/proctor.ts` (new, ProctorU/Examity
  client).
- **Agent:** general-purpose

### PF-025: Roy Enterprise SLA program + private registry + indemnification
- **Area:** backend-node / docs / infra
- **State:** partial — `roy-os.tsx`, `backend-node/src/modules/enterprise/`
  exist as UI shells; no LTS policy doc, no private registry infra, no
  SLA program. (PLATFORM-VISION §1.7, Tier 2 #35. Note: governance docs
  required first — PF-005.)
- **Acceptance:**
  1. SLA-backed support: named support engineer, 1h P0 / 4h P1 / NBD
     P2; dedicated Slack; quarterly architecture review (depends on
     PF-005).
  2. Private registry: on-prem or VPC-hosted Roy Cloud mirror; airgap
     install for regulated industries.
  3. Annual third-party penetration test of the RoyCSS runtime.
  4. SOC 2 Type II attestation (target Q1 2027).
  5. IP indemnification up to $1M per contract.
  6. 8 hours of instructor-led training per year; 10 Roy Academy seats.
- **Files:** `backend-node/src/modules/enterprise/service.ts` (extend),
  `infrastructure/terraform/main.tf` (private registry),
  `docs/ENTERPRISE-SLA.md` (new), `docs/INDEMNIFICATION.md` (new).
- **Agent:** general-purpose

### PF-026: Roy Themes vertical-specific store (10 verticals)
- **Area:** frontend / backend-node
- **State:** partial — `theme-system.tsx` ships 10 first-party presets;
  no vertical-specific theme store. (PLATFORM-VISION §1.11, Tier 2 #31.)
- **Acceptance:**
  1. 10 verticals: Healthcare, Banking, SaaS, Education, E-commerce,
     Government, Gaming, Media, Legal, Real Estate.
  2. Each theme: complete token set + component overrides + motion pack,
     reviewed for industry compliance (HIPAA-aligned colors for
     Healthcare, WCAG-enhanced contrast for Government).
  3. Sold one-time per project ($49–$299) or via Roy Cloud subscription
     ($19/month all-you-can-eat); listed on Roy Marketplace (PF-020).
  4. 5 first-party theme packs commissioned for launch (Healthcare /
     SaaS / Fintech / Education / E-commerce) — shared deliverable with
     PF-020.
- **Files:** `src/components/roycss/pro/theme-system.tsx` (extend),
  `src/app/themes/page.tsx` (new), `backend-node/src/modules/themes/service.ts`
  (extend), `dist/themes/*.css` (new).
- **Agent:** general-purpose

### PF-027: Roy Motion Library Premium (50 sequences + 30 springs + 20 page transitions + gesture motion + motion tokens)
- **Area:** motion
- **State:** partial — `motion-library.tsx` ships 60 free presets; no
  premium tier. (PLATFORM-VISION §1.12, Tier 2 #32.) Note: gesture +
  page transitions + motion tokens overlap with PF-017; this item is
  the paid gating.
- **Acceptance:**
  1. 50 choreographed sequences (onboarding, checkout, dashboard-load
     reveals).
  2. 30 spring physics presets ("Playful", "Confident", "Snappy",
     "Calm", "Energetic") with parameters exposed.
  3. 20 View Transitions API page transitions with cross-document.
  4. Gesture-driven motion (magnetic cursors, parallax-on-pointer,
     tilt-on-gyroscope, drag-with-physics).
  5. Motion tokens (integrate with Roy Cloud for team-wide motion
     consistency).
  6. Paid gating: one-time $99 or included in Pro Components
     subscription; license-check via PF-010.
- **Files:** `src/components/roycss/pro/motion-library.tsx` (extend
  + gating), `backend-node/src/modules/motion/service.ts` (license
  check).
- **Agent:** general-purpose

### PF-028: Headless / Styled split (`@roycss/headless` + `@roycss/styled` + CVA compiled variants + 100+ components)
- **Area:** components
- **State:** not started — `src/components/ui/*` is a shadcn/ui fork
  (Radix + Tailwind runtime); no `@roycss/headless` package. (V2 §5,
  F4/F5, Tier 2 #26.)
- **Acceptance:**
  1. `packages/@roycss/headless/` — framework-agnostic primitives
     (DOM + a11y, zero CSS).
  2. `packages/@roycss/styled/` — styled component library, 100+
     components across 12 categories, CVA compiled variant system (not
     runtime `cva`).
  3. Anchor-First Overlay System (r-menu, r-tooltip, r-popover,
     r-dropdown, r-combobox) using Popover API + CSS Anchor Positioning
     + 1.2KB polyfill (F4).
  4. Scope-Encapsulated Components: `@scope` blocks for all 50 patterns
     + `@roycss-scoped()` helper + codemod from CSS Modules / BEM /
     styled-components (F5).
  5. Container-Adaptive Components: `@roycss-container` directive +
     codemod from viewport media queries (F10).
- **Files:** `packages/@roycss/{headless,styled}/` (new, requires
  PF-042 monorepo).
- **Agent:** general-purpose

### PF-029: Composable Effect Recipes (`@roycss-recipe/*` npm org + composition engine)
- **Area:** recipes / tooling
- **State:** partial — `recipes-section.tsx` and
  `backend-node/src/modules/recipes/` exist with 12 recipes; no recipe
  format spec, no `@roycss-recipe/*` org, no composition engine. (F15,
  Tier 2 #30.)
- **Acceptance:**
  1. Recipe format spec: a JSON/YAML manifest describing effect
     composition (which effects, which tokens, which overrides).
  2. `@roycss-recipe/*` npm org for community recipe packs.
  3. Composition engine: `roycss recipe build <recipe>` emits CSS +
     framework-specific code (React/Vue/Svelte/Angular).
  4. V1 effects migrated to a recipe pack as a migration example.
  5. Recipe registry in Roy Cloud (PF-019) for sharing.
- **Files:** `packages/@roycss-recipes/` (new, requires PF-042),
  `cli/index.js` (add `recipe` command),
  `backend-node/src/modules/recipes/service.ts` (extend).
- **Agent:** general-purpose

### PF-030: Token Type System (`@property`-registered tokens + TS types emitted + static check + W3C DTCG JSON)
- **Area:** tokens / tooling
- **State:** partial — `src/lib/design-tokens.ts` exports typed tokens;
  `@property` used in CSS; no W3C DTCG JSON emission, no static compiler
  check. (F9, Tier 2 #36, LABS-34 §5.4.)
- **Acceptance:**
  1. Every token registered with `@property` (syntax, inherits, initial
     value).
  2. TypeScript types emitted (`dist/tokens.d.ts`); static compiler
     check that consumer code references only valid tokens.
  3. W3C DTCG JSON emission (`dist/tokens.dtcg.json`).
  4. "Token Portability" guarantee — no RoyCSS-specific token format
     (LABS-34 §5.4).
  5. "Fashion-resistant tokens" stability commitment published in
     `docs/TOKEN-STABILITY.md` (LABS-36 §6.8).
- **Files:** `src/lib/design-tokens.ts` (extend),
  `dist/tokens.{d.ts,dtcg.json}` (new), `docs/TOKEN-STABILITY.md` (new),
  `scripts/validate-tokens.ts` (new).
- **Agent:** general-purpose

### PF-031: Crowdsourced translation (Crowdin/Weblate) + i18n message catalog + RTL via logical properties + telemetry SDK + public dashboard + cross-browser CI
- **Area:** i18n / observability / CI
- **State:** partial — `tests/i18n/` runs RTL + OKLCH audits; `i18n-rtl`
  ADR exists; no translation platform, no message catalog, no telemetry
  SDK, no public dashboard, no BrowserStack CI. (LABS-30 §7.9, R11,
  V2 §13.3, Tier 2 #37/38/39/40.)
- **Acceptance:**
  1. `src/messages/*.json` message catalog; Crowdin or Weblate
     integration; community translation workflow.
  2. RTL via logical properties — audit + fix any remaining
     `translateX` marquee usage (ENTERPRISE-REVIEW R6).
  3. `packages/@roycss/telemetry/` — opt-in telemetry SDK with public
     dashboard at `/stats` (privacy-first: no third-party trackers).
  4. Cross-browser testing in CI (BrowserStack or Playwright
     multi-browser) — Chrome, Firefox, Safari, Edge (Baseline 2024
     matrix).
- **Files:** `src/messages/*.json` (new), `packages/@roycss/telemetry/`
  (new), `src/app/stats/page.tsx` (new), `.github/workflows/cross-browser.yml`
  (new).
- **Agent:** general-purpose

---

## P3 — v2 roadmap (themed, grouped from 96 HIGH-priority NOT-STARTED items)

> Items 32–47 below group the 96 HIGH-priority NOT-STARTED Tier 1 items
> from `PENDING-FEATURES-AUDIT.md` Section 19 into ~16 themed
> engagements. Each is a multi-quarter effort; the V2 monorepo
> (PF-032) is the foundation for most downstream items.

### PF-032: V2 monorepo + build pipeline (Lightning CSS, AOT/JIT/runtime, multi-surface token emission)
- **Area:** tooling / build
- **State:** not started — no `packages/` folder, no `turbo.json`, no
  `.changeset`, no workspace declaration. (V2 §1.2-1.4, F1-F10, Tier 1 #1.)
- **Acceptance:**
  1. `packages/@roycss/*` — 12+ packages: `core`, `cli`, `postcss`,
     `vite`, `webpack`, `rspack`, `rollup`, `esbuild`, `astro`, `next`,
     `nuxt`, `remix`, `sveltekit`, `headless`, `react`, `vue`, `svelte`,
     `solid`, `angular`, `styled`, `motion`, `themes`, `icons`,
     `devtools`, `vscode`, `a11y`, `codemods`, `test`, `rum`, `ai`,
     `tokenstudio`.
  2. Bun workspaces + Turborepo + Changesets versioning.
  3. `roycss` meta-package re-exports the common path for one-install.
  4. Build pipeline: Token resolver (Style-Dictionary fork, Rust-backed
     via `napi-rs`) → Plugin transforms (AST → CSS) → Lightning CSS
     (minify, cascade-layer assignment, syntax lowering for legacy
     browsers, unused-CSS removal at consumer build) → Codegen (`.d.ts`,
     JSON autocomplete data, CLI manifests, React/Vue/Svelte type stubs)
     → Bundle split (roycss-base.css always loaded, per-component lazy,
     roycss-theme-{name}.css swappable) → Critical CSS (streaming SSR
     for SSR frameworks).
  5. Rendering modes auto-selected per route: AOT (default, Lightning
     CSS scans source, emits only used classes), JIT (dev, persistent
     on-disk cache, <50ms rebuild per HMR), Runtime (escape hatch,
     `@roycss/runtime` ~3KB gzip for CMS-driven class names).
  6. Dependencies: Bun ≥1.2, TypeScript ≥5.7, Lightning CSS ≥1.30, Rust
     ≥1.83 (for napi-rs), Node ≥20.
- **Files:** `packages/@roycss/*` (new), `turbo.json` (new),
  `.changeset/` (new), root `package.json` (workspaces),
  `scripts/build-package.ts` (rewrite to use Lightning CSS).
- **Agent:** Plan (multi-quarter engagement)

### PF-033: Intent-Class Compiler / RoyLang + Intent-Level Pattern Abstractions
- **Area:** compiler / language
- **State:** not started. (F1, LABS-26, LABS-31, LABS-36 §6.7, Tier 1 #2.)
  Top of the dependency graph (per PENDING-FEATURES-AUDIT §20).
- **Acceptance:**
  1. `packages/@roycss/compiler/` — Intent-Class Compiler (lex → parse →
     resolve → compose → optimize → emit → validate).
  2. RoyLang language specification: intent verbs, block kinds,
     modifiers, scoping, escape hatches, types/validation.
  3. RoyLang vs raw CSS examples (card, grid, button, animated hover,
     form layout).
  4. Intent-Level Pattern Abstractions (LABS-31): `r-card`, `r-btn`,
     `r-dashboard`, `r-form`, `r-modal`, `r-table`, `r-pricing`,
     `r-landing` — 50 patterns × 10 variants × 8 modifiers × 20
     behaviors.
  5. Override contract: variant composition + token substitution +
     override hooks preserve 100% flexibility.
  6. Migration from raw CSS / Tailwind / Bootstrap / CSS-in-JS /
     incremental adoption.
  7. Watch mode + incremental compilation + source maps + DevTools.
  8. AI-Native Authoring (LABS-36 §6.7): intent not property, cures
     imposter syndrome / math anxiety / AI-reviewability.
- **Files:** `packages/@roycss/{compiler,roylang}/` (new),
  `docs/roylang/` (new), `scripts/migrate-to-roylang.ts` (new).
- **Agent:** Plan (research-grade work)

### PF-034: Plugin API contract + 5 official first-party plugins + plugin marketplace
- **Area:** plugin system
- **State:** not started — only `plugin-hub.tsx` UI and
  `backend-node/src/modules/plugin-hub/` curated catalog exist. (V2
  §12, LABS-35 §3, R13, Tier 1 #3.)
- **Acceptance:**
  1. `packages/@roycss/core/plugin-api.ts` — `roycss.plugin({ name,
    effects, tokens, transformers })`.
  2. Plugin lifecycle: `init`, `build`, `emit`, `post-build` hooks.
  3. What plugins cannot do (security constraints): no DB access, no
     raw filesystem, no network in build sandbox.
  4. Discovery: npm `roycss-plugin-*` convention; `roycss plugins
     search/install/list` CLI.
  5. 5 official first-party plugins: `rtl`, `print`, `a11y-strict`,
     `brand-colors`, `tailwind-compat`.
  6. Plugin marketplace at `roycss.dev/plugins` — community plugins,
     vetted (curation + security review per PF-020's pipeline).
- **Files:** `packages/@roycss/core/plugin-api.ts` (new),
  `packages/@roycss/plugin-{rtl,print,a11y-strict,brand-colors,tailwind-compat}/`
  (new), `src/app/plugins/page.tsx` (new).
- **Agent:** Plan

### PF-035: Cascade Constitution + Scope-Encapsulated + Container-Adaptive + Anchor-First Overlay (V2 component layer)
- **Area:** css / components
- **State:** partial — `css-layers.tsx` visualizes layers;
  `anchor-positioning.tsx` tool exists; `container-query-builder.tsx`
  tool exists; shadcn/ui uses Radix Popover. (F3, F4, F5, F10, LABS-33
  §8.3, LABS-36 §6.1, Tier 1 #25.)
- **Acceptance:**
  1. **Cascade Constitution (F3):** `@layer` defaults ordered `tokens →
     reset → base → utilities → components → variants → app`;
     build-time enforcement; `@roycss-escape` annotation; `strict:
     false` mode. `:where()` for low-specificity wrappers. Lint against
     `!important`; audit + remove existing `!important`
     (`scripts/audit-important.ts`).
  2. **Scope-Encapsulated Components (F5):** `@scope` blocks for all 50
     patterns; `@roycss-scoped()` helper; codemod from CSS Modules /
     BEM / styled-components. Locality-by-default (LABS-36 §6.1).
  3. **Container-Adaptive Components (F10):** all 50 patterns
     container-adaptive; `@roycss-container` directive; codemod from
     viewport media queries.
  4. **Anchor-First Overlay System (F4):** `r-menu`, `r-tooltip`,
     `r-popover`, `r-dropdown`, `r-combobox` with Popover API + CSS
     Anchor Positioning + 1.2KB polyfill.
- **Files:** `packages/@roycss/{core,styled}/` (extend after PF-032),
  `scripts/audit-important.ts` (new), `src/app/roycss.css` (rewrite with
  `@layer`).
- **Agent:** general-purpose (after PF-032 lands)

### PF-036: Self-healing CSS linter + VS Code LSP + `roycss lint` + auto-fix + community rule API
- **Area:** tooling / vscode
- **State:** partial — `roycss doctor` exists;
  `vscode-extension/src/completion-provider.ts` + `hover-provider.ts`
  exist; no LSP, no diagnostics, no dead-class detection, no a11y hints,
  no migration code actions, no `roycss lint`, no auto-fix, no
  community rule API. (F14, LABS-32 §8, LABS-36 §6.5, V2 §10.1, R2,
  Tier 1 #29.)
- **Acceptance:**
  1. `packages/@roycss/lint/` — self-healing CSS linter (AST-based,
     objective correctness checks per LABS-36 §6.5).
  2. `@roycss/vscode` LSP: diagnostics, dead-class detection, a11y
     hints, migration code actions, inline cascade preview (LABS-29
     §2.2).
  3. `roycss lint` CLI — runs the linter; `--fix` applies auto-fixes.
  4. `roycss doctor` — full audit (already exists; extend with linter
     rules).
  5. Community rule API — third-party rule packages.
  6. Class Usage Heatmap in IDE (decoration provider).
- **Files:** `packages/@roycss/{lint,vscode}/` (new, requires
  PF-032), `vscode-extension/src/` (rewrite as LSP server),
  `cli/index.js` (add `lint` command).
- **Agent:** general-purpose (after PF-032 lands)

### PF-037: Performance Observable Framework (`roycss perf:check/overlay/ci` + Playwright route testing + PerformanceObserver source-map attribution)
- **Area:** tooling / perf
- **State:** partial — `perf/benchmark.ts`, `perf/regression.test.ts`,
  `roy-profiler.tsx` exist; no `roycss perf:*` commands, no Playwright
  route testing, no PerformanceObserver source-map attribution. (F12,
  LABS-33 §10, Tier 1 #11.)
- **Acceptance:**
  1. `roycss perf:check` — CLI one-shot perf audit.
  2. `roycss perf:overlay` — runtime overlay showing paint / layout /
     composite cost per element.
  3. `roycss perf:ci` — CI-integrated regression tests with source-map
     attribution (which CSS rule caused the long paint).
  4. Playwright route testing — scriptable scenarios for hot paths.
  5. `PerformanceObserver` integration with source-map attribution.
  6. 15-engineer-week implementation roadmap tracked in
     `docs/plans/PERFORMANCE-ROADMAP.md`.
  7. CSS-Aware CI Gate (50-ORIG 8.6) — `!important` / `z-index` /
     duration gate.
- **Files:** `cli/index.js` (add `perf:check`, `perf:overlay`,
  `perf:ci`), `packages/@roycss/perf/` (new),
  `docs/plans/PERFORMANCE-ROADMAP.md` (new).
- **Agent:** general-purpose

### PF-038: Browser DevTools panel + Roy Inspector Chrome extension + Utility Explorer
- **Area:** browser-extension
- **State:** not started — only the VSCode extension exists; no Chrome
  / Edge / Firefox / Safari extension; no DevTools panel. (PLATFORM-VISION
  §1.9-1.10, V2 §10.2, R2, Tier 1 #4.)
- **Acceptance:**
  1. New repo `browser-extension/` (MV3 manifest + DevTools panel) for
     Chrome + Firefox + Edge + Safari.
  2. **RoyCSS tab** in DevTools — shows which RoyCSS classes are
     applied to the selected element, which custom properties are
     inherited, which token each value resolves to.
  3. **Token inspector** — visualize the `--roycss-*` custom-property
     tree; edit live; sync back to Roy Cloud (PF-019).
  4. **Effect debugger** — pause RoyMotion animations; scrub timelines;
     toggle reduced-motion; isolate scroll-driven animations.
  5. **Bundle analyzer** — RoyCSS portion of the page's CSS by category
     and effect.
  6. **AI panel** — right-click → "Ask Roy AI" → audit / explain /
     refactor (uses PF-021).
  7. **Utility Explorer** (PLATFORM-VISION §4.6) — hover any class →
     popover with CSS + perf + a11y + used-by + related.
  8. **Roy Inspector** — free Chrome extension for inspecting any
     website's CSS (lead-gen funnel for the ecosystem).
  9. **Cascade Genealogy Inspector** (50-ORIG 1.1), **Specificity
     Heatmap Overlay** (1.2), **CSS Time Travel** (1.3), **Property
     Diff Inspector** (1.5), **Custom Property Substitution
     Visualizer** (1.6), **Layout Constraint Conflict Detector**
     (1.7), **Computed Style Snapshot Diff** (1.8) — all 10
     debugging/diagnostics ideas from 50-ORIGINAL-FEATURES Category 1.
- **Files:** `browser-extension/` (new repo), `vscode-extension/`
  (coordinate).
- **Agent:** Plan (separate engagement)

### PF-039: Roy Studio (Tauri-based visual builder — Figma meets Webflow, exports RoyCSS)
- **Area:** desktop-app
- **State:** not started — only web-based `playground-panel.tsx` and
  `visual-studio.tsx` (token editor) exist; no Tauri desktop app.
  (PLATFORM-VISION §1.3, Tier 1 #14.)
- **Acceptance:**
  1. New repo `studio/` (Tauri 2.0, 30MB installer, macOS + Windows +
     Linux).
  2. Canvas — drag-and-drop layout with real CSS Grid / Flexbox /
     container-query primitives.
  3. Token editor — visual OKLCH palette generation, contrast checking
     against WCAG 2.2 AA/AAA in real time, automatic tint/shade via
     `color-mix()`.
  4. Effect picker — drag any of the 1,749 RoyCSS effects onto an
     element; tune duration/easing/delay/iteration via sliders.
  5. Component composer — compose Pro Components visually; Studio
     writes framework-specific bindings (React/JSX, Vue/SFC, Svelte,
     Angular, Astro).
  6. Export — one-click to RoyCSS HTML/CSS, React + RoyCSS (Next.js /
     Vite / Remix), Vue + RoyCSS, Figma library (sync back via
     PF-018), Roy Cloud theme (PF-019).
  7. Live preview — iframe, real-time, device frames, dark/light
     toggle, reduced-motion toggle, `prefers-contrast: high` toggle.
  8. Local-first with optional Roy Cloud sync.
  9. Read-only web companion for stakeholders.
  10. Free tier (single project, local-only); Team tier ($19/mo Pro,
      $49/mo Team) drives revenue.
- **Files:** `studio/` (new repo, Tauri + React).
- **Agent:** Plan (separate engagement)

### PF-040: Roy Commercial Cloud — Roy Cloud V2.3 (SSO, audit, theme sync, Git-backed token repos, multi-cursor, CDN hosting) + Roy Marketplace payments + Roy Enterprise SLA + private registry
- **Area:** backend / infra / commercial
- **State:** partial — `backend-node/src/modules/{cloud,marketplace,enterprise}/`
  exist; no SSO, no Git-backed token repos, no multi-cursor, no CDN
  theme hosting, no payouts, no private registry. (PLATFORM-VISION
  §1.4-1.5-1.7, V2 §16.9, Tier 1 #15/16/19/35.) Note: PF-019/020/025
  are the V1 on-ramps; this item is the V2.3 commercial consolidation.
- **Acceptance:**
  1. Roy Cloud V2.3: SSO (Google / GitHub / SAML), audit logs (per
     PF-019), theme sync, Git-backed token repos, multi-cursor editing,
     CDN theme hosting.
  2. Roy Marketplace payments + creator onboarding + payouts (per
     PF-020).
  3. Roy Enterprise SLA + private registry + indemnification (per
     PF-025).
  4. Roy Cloud free tier (1 project, 3 themes, public-only) drives
     adoption; paid tiers wired to Stripe (PF-010).
- **Files:** `backend-node/src/modules/{cloud,marketplace,enterprise, billing,subscriptions,usage}/service.ts`
  (consolidate), `infrastructure/terraform/main.tf`.
- **Agent:** Plan (multi-quarter, depends on PF-010/019/020/025)

### PF-041: Roy AI hybrid retrieval + `@roycss/ai` + `roycss.rules.md` + training pairs + AI conformance suite (V2 consolidation)
- **Area:** AI / tooling
- **State:** see PF-021 (V1 on-ramp). This item is the V2
  consolidation: integrated AI layer across CLI, Studio, Cloud,
  Marketplace, DevTools, Inspector. (PLATFORM-VISION §1.6, V2 §8.3,
  LABS-32 §8, LABS-27 §7.2, F11, Tier 1 #17.)
- **Acceptance:**
  1. Same as PF-021 (vector index + `@roycss/ai` + `roycss.rules.md` +
     training pairs + AI conformance suite).
  2. Integrated across every surface: CLI (`roycss generate
     from-prompt`), Studio (in-canvas AI), Cloud (token suggestions),
     Marketplace (AI-assisted listing descriptions), DevTools ("Ask Roy
     AI"), Inspector (AI audit).
  3. 2027-2028 AI layer roadmap (LABS-27 §7.2).
- **Files:** see PF-021.
- **Agent:** Plan (after PF-021 + PF-032)

### PF-042: Component Genome + `/roycss.manifest.json` + Effect maturity tags + `API.md` public API surface + CI gate
- **Area:** data / docs / CI
- **State:** partial — `dist/effects.json`, `dist/class-index.json`,
  `dist/pro-components.json`, `dist/version-manifest.json` exist but
  are not unified; no maturity tags; no `API.md` (per PF-013 V1
  stopgap). (PLATFORM-VISION §4.3, LABS-30 §4.1/§7.1/§3.2, LABS-35
  §2/§4.4, Tier 1 #6/19/21.)
- **Acceptance:**
  1. `public/roycss.manifest.json` — unified machine-readable manifest
     (every effect, category, custom props, preview type, maturity,
     description, usage HTML snippet). Generated by
     `scripts/gen-manifest.ts`.
  2. `dist/roycss.manifest.d.ts` — TypeScript declarations for the
     manifest.
  3. Effect maturity tags (`experimental` / `stable` / `deprecated`) in
     `src/lib/effect-taxonomy.ts` + filter UI in catalog.
  4. Stable naming convention `roycss-<category>-<verb>-<modifier>`
     enforced by build (`scripts/validate-effects.ts`).
  5. `usage` field with canonical HTML snippet per effect in the
     manifest.
  6. Component Genome (per PF-022 V1 on-ramp) integrated into the
     manifest.
  7. `API.md` (per PF-013) + CI gate (`.github/workflows/api-gate.yml`)
     — no `stable` name removed without major bump.
- **Files:** `public/roycss.manifest.json` (new),
  `scripts/gen-manifest.ts` (new), `dist/roycss.manifest.d.ts` (new),
  `src/lib/effect-taxonomy.ts` (maturity + usage fields),
  `API.md` (per PF-013).
- **Agent:** general-purpose

### PF-043: Storybook addon + Chromatic visual regression + Cross-browser (BrowserStack) CI + telemetry dashboard
- **Area:** tooling / observability / CI
- **State:** partial — `tests/e2e/` uses Playwright; no Chromatic; no
  Storybook addon; no BrowserStack. See PF-031 for the V1 on-ramp.
  (R8, V2 §13.1/13.3, Tier 1 #23.)
- **Acceptance:**
  1. `packages/@roycss/storybook-addon/` — Storybook addon with MDX
     docs + RoyCSS theming.
  2. Chromatic visual regression in CI on every PR.
  3. Cross-browser testing (BrowserStack or Playwright multi-browser)
     — Chrome, Firefox, Safari, Edge.
  4. Telemetry SDK + public dashboard (per PF-031).
- **Files:** `packages/@roycss/storybook-addon/` (new),
  `.github/workflows/{visual-regression,cross-browser}.yml` (new).
- **Agent:** general-purpose

### PF-044: V2 governance + enterprise — WCAG 3.0 readiness + SLSA Level 3 + SOC 2 Type II + LTS program + 5-tier RoyCSS Working Group + RoyCSS Conf
- **Area:** governance / enterprise
- **State:** partial — PF-005 ships the initial governance docs; this
  item is the V2.3 enterprise + governance consolidation. (V2
  §16.6/16.10/16.11/16.13, ENTERPRISE-REVIEW, LABS-35 §5, R15,
  PLATFORM-VISION §6.4, Tier 1 #10/15.)
- **Acceptance:**
  1. **WCAG 3.0 readiness** — research; APCA-like model (LABS-27 R8).
  2. **SLSA Level 3 build provenance** — hermetic builds, signed
     artifacts (V2 §16.11).
  3. **SOC 2 Type II audit** — target Q1 2027 (V2 §16.13).
  4. **LTS program launch** — per PF-005's `docs/LTS.md`, with
     `backport-action` GitHub bot for 18-month backporting.
  5. **RoyCSS Working Group** — 5 core maintainers + 9-member steering
     committee, public meetings + notes (V2 §16.6, R15).
  6. **RoyCSS Conf** — first annual conference (500 attendees, virtual
     + hybrid), sponsored by Platinum and Technology Partner sponsors
     (PLATFORM-VISION §6.4).
  7. Release cadence: monthly minor, weekly patch, LTS branches (V2
     §16.4, LABS-35 §8) — documented in `docs/RELEASE-CADENCE.md`.
  8. Reproducible-build hash published in `docs/REPRODUCIBLE-BUILD.md`.
  9. Renovate Bot config + Rollback runbook (`docs/ROLLBACK-RUNBOOK.md`).
- **Files:** `docs/{WCAG-3.0-PLAN,REPRODUCIBLE-BUILD,RELEASE-CADENCE,ROLLBACK-RUNBOOK}.md`
  (new), `renovate.json` (new).
- **Agent:** Plan (multi-year)

### PF-045: RoyCSS Conf + Sponsorship program polish + Community / contributors / marketing
- **Area:** community / marketing
- **State:** partial — `pricing-section.tsx` + `featured-companies.tsx`
  sponsorship modal exists (says "Stripe coming soon" — wired in
  PF-010); no RoyCSS Conf; no contributor ladder beyond
  `docs/CONTRIBUTING.md`. (PLATFORM-VISION §5 + §6.4, LABS-28 §3,
  Tier 1 #46/47.)
- **Acceptance:**
  1. RoyCSS Conf — first annual conference (Month 32 of GTM,
     PLATFORM-VISION §6.4): 500 attendees, virtual + hybrid, sponsored
     by Platinum + Technology Partner sponsors.
  2. Contribution ladder (Triager → Contributor → Collaborator →
     Maintainer → Steering Committee) documented in
     `docs/CONTRIBUTING.md` extension.
  3. Good-first-issue bot + named maintainers per area.
  4. Public 12-month roadmap at `src/app/roadmap/page.tsx` distilled
     from this file.
  5. Starter template gallery (`roycss.dev/templates`,
     `npm create roycss@latest --template`).
- **Files:** `docs/CONTRIBUTING.md` (extend), `src/app/roadmap/page.tsx`
  (new), `src/app/templates/page.tsx` (new), `cli/index.js` (add
  `create` command).
- **Agent:** general-purpose

### PF-046: v3 research — Pure-CSS Behavioral Primitives + WebGPU + Spatial CSS + Time-Aware CSS + Layout Intent API + Manifest-Driven Styling + CSS Trigonometry Layouts
- **Area:** research
- **State:** not started (research directions). (LABS-26-36 long-term,
  FIRST-PRINCIPLES-REDESIGN R1-R10, Tier 1 #44.)
- **Acceptance:**
  1. **Pure-CSS Behavioral Primitives** (LABS-27 R3) — tabs / accordions
     / dropdowns / modals via `:has()`, `<details>`, `popover`,
     `<dialog>`; replace Radix JS-driven with pure-CSS in
     `packages/@roycss/headless/pure-css/`.
  2. **WebGPU-Accelerated Effects** (LABS-27 R6) — particle systems,
     fluid simulations, complex shaders in
     `src/components/roycss/effects/webgpu-*`.
  3. **Spatial CSS** (LABS-27 §5.2) — WebXR tokens / patterns for
     depth / parallax / gaze.
  4. **Time-Aware CSS** (FIRST-PRINCIPLES R1) — `@timeline` directive,
     cookie-backed custom properties.
  5. **Layout Intent API** (FIRST-PRINCIPLES R2) — `r-layout:sidebar-with-sticky-header`,
     `r-layout:holy-grail`, `r-layout:dashboard-3col`.
  6. **Manifest-Driven Styling** (FIRST-PRINCIPLES R4) — `roycss.toml`
     project manifest.
  7. **CSS Trigonometry Layouts** (FIRST-PRINCIPLES R5) — `sin()`,
     `cos()`, `tan()` for radial / organic grids.
  8. **AI-Generated Pattern Catalog** (LABS-27 R9) — AI generates
     `r-pricing-card` on demand; added to project catalog.
  9. **Real-Time Collaboration Tokens** (LABS-27 R7) — Figma ↔ IDE ↔
     running-app sync over WebSocket.
  10. **Interface Contracts** (LABS-27 §7.4, 2030-2035) — long-term
      research.
- **Files:** `research/` (new top-level dir), `packages/@roycss/headless/pure-css/`
  (new), `src/components/roycss/effects/webgpu-*` (new).
- **Agent:** Plan (research-grade)

### PF-047: LABS-28 deprecation decision — cut proposal vs V2 accretive path
- **Area:** product decision
- **State:** open product decision. LABS-28 proposes deleting ~520
  effects (cut to 180 across 6 categories), deleting the shadcn/ui
  fork, deleting the CLI, deleting the color customizer UI, deleting
  the favorites system, deleting framework adapters, deleting VS Code
  snippets, deleting the section scrollbar, deleting the scroll-to-top
  button. The current codebase ships 1,569+ effects across 20+
  categories (grew, not shrunk). (LABS-28, Tier 3 #45.)
- **Acceptance:**
  1. Product decision documented: pursue the V2 accretive path (per
     PLATFORM-VISION / V2-BLUEPRINT) OR the LABS-28 amputation path.
  2. If accretive: explicitly mark LABS-28 items as `[DEPRECATED]` in
     `docs/LABS-28-DELETE-HALF.md` with a "decision: not pursuing" note.
  3. If amputation: produce the cut plan, schedule the deletions, ship
     the codemods (PF-015) for users who depend on removed items.
  4. Either way: this decision must be made before further V2
     investment (per PENDING-FEATURES-AUDIT §22 final note #5).
- **Files:** `docs/LABS-28-DELETE-HALF.md` (decision annotation),
  `docs/PRODUCT-DIRECTION.md` (new — the decision doc).
- **Agent:** general-purpose (product owner decision; engineering
  implements whichever path is chosen)

---

## Cross-document dependencies (preserved from PENDING-FEATURES-AUDIT §20)

The dependency graph top is the **Intent-Class Compiler / RoyLang**
(PF-033). Many downstream items depend on it:

```
Intent-Class Compiler (PF-033)
├── Cascade Constitution + Scope + Container + Anchor (PF-035)
├── Self-Healing Linter (PF-036)
├── CSS-Aware CI Gate (PF-006 + PF-037)
├── Intent-Level Pattern Abstractions (PF-033)
├── AI-Native Authoring (PF-041)
└── Composable Effect Recipes (PF-029)

V2 Monorepo (PF-032)
├── @roycss/headless + @roycss/styled (PF-028 + PF-035)
├── @roycss/motion (PF-017 + PF-027)
├── @roycss/a11y (PF-023)
├── @roycss/rum (PF-023)
├── @roycss/ai (PF-021 + PF-041)
├── @roycss/lint (PF-036)
├── @roycss/vscode LSP (PF-036)
├── @roycss/devtools panel (PF-038)
├── @roycss/tokenstudio (PF-018)
└── Build plugins (PF-016)

Public API surface (PF-013 + PF-042)
├── Effect maturity tags (PF-042)
├── Plugin API (PF-034)
├── Codemod library (PF-015)
└── Machine-readable manifest (PF-042)

WCAG 2.2 AA audit (PF-004)
├── VPAT 2.4 (PF-004)
├── Per-effect a11y tag (PF-004)
├── Effects allowlist (PF-004)
└── @roycss/a11y CI build-fail (PF-023)

backend-go port (PF-008) — blocked by:
├── PF-003 (PostgreSQL + Redis + S3 + workers)
├── PF-007 (contract tests become acceptance tests)
└── Go 1.23+ toolchain outside this sandbox
```

---

## Source-to-PF-NNN cross-reference (for traceability)

| Source doc | Section / Item | PF-NNN |
|---|---|---|
| AUDIT-1 | F1, F2, F8 (DONE) | (removed — DONE per worklog FIX-F1-F2 + Task 5) |
| AUDIT-1 | F3 (requireAuth) | PF-001 |
| AUDIT-1 | F4 (analytics mock) | PF-012 |
| AUDIT-1 | F5 (mcp mock) | PF-012 |
| AUDIT-1 | F6 (edge mock) | PF-012 |
| AUDIT-1 | F7 (plugin-hub mock) | PF-012 |
| AUDIT-1 | F9 (live messages) | PF-012 |
| AUDIT-1 | F10/F11/F12 (Playwright mocks) | PF-012 |
| AUDIT-1 | F13 (BACKEND-COMPLETION-REQUIREMENTS stale) | superseded by this file |
| AUDIT-1 | F14 (refactor mock) | PF-012 |
| AUDIT-1 | F15 (scaffold/generator mock) | PF-012 |
| AUDIT-1 | F16 (health effectsCount hardcoded) | PF-012 |
| AUDIT-1 | F17 (TODO A1-A9 unchecked) | PF-009 + P1 |
| AUDIT-1 | F18 (V2 vision 96 items) | PF-032 → PF-047 (P3 themed) |
| ROYCSS_BACKEND_TODO A1 | Registry single source | PF-009 |
| ROYCSS_BACKEND_TODO A2 | Authz granularity | PF-001 |
| ROYCSS_BACKEND_TODO A3 | API keys | PF-002 |
| ROYCSS_BACKEND_TODO A4 | OpenAPI generation | PF-009 |
| ROYCSS_BACKEND_TODO A5 | Rate-limit tiers | PF-009 |
| ROYCSS_BACKEND_TODO A6 | Audit log coverage | PF-009 |
| ROYCSS_BACKEND_TODO A7 | Worker separation | PF-009 |
| ROYCSS_BACKEND_TODO A8 | Testing | PF-007 |
| ROYCSS_BACKEND_TODO A9 | Observability | PF-009 |
| ROYCSS_BACKEND_TODO §B | Go production target | PF-008 (single batch) |
| ROYCSS_BACKEND_TODO §B8/B9 | Marketplace/Billing hardening | PF-010 + PF-040 |
| ROYCSS_SECURITY_REPORT §3/§10 | requireAuth / API keys / CSRF | PF-001 + PF-002 |
| ROYCSS_PERFORMANCE_REPORT §3 | Target SLAs | PF-003 + PF-037 |
| ROYCSS_OBSERVABILITY §7 | Per-route metrics + requestId | PF-009 |
| ROYCSS_DEPLOYMENT §3-5 | Cloud Run + CI/CD | PF-003 |
| ROYCSS_TEST_REPORT §3-5 | Test plan | PF-007 |
| ROYCSS_MIGRATION_GUIDE §5 | Module-by-module cutover | PF-008 |
| ROYCSS_DATABASE_ARCHITECTURE §2 | PostgreSQL target | PF-003 + PF-008 |
| ROYCSS_API_SPECIFICATION §3 | API keys TODO | PF-002 |
| BACKEND-COMPLETION-REQUIREMENTS §2.1 | 30 Prisma models | superseded (45 now exist) |
| BACKEND-COMPLETION-REQUIREMENTS §2.2 | 14 external services | PF-003 + PF-012 + PF-019 + PF-021 |
| BACKEND-COMPLETION-REQUIREMENTS §2.3 | 62 product cards | superseded (37 now use useBackendData, per FIX-F1-F2) |
| BACKEND-COMPLETION-REQUIREMENTS §2.4 | Auth frontend | DONE (FINAL-VERIFICATION §4.4) |
| BACKEND-COMPLETION-REQUIREMENTS §2.5 | Production infra | PF-003 |
| BACKEND-COMPLETION-REQUIREMENTS §2.6 | Worker queue | PF-003 + PF-009 |
| BACKEND-COMPLETION-REQUIREMENTS §2.7 | Testing | PF-007 |
| BACKEND-COMPLETION-REQUIREMENTS §2.8 | CI/CD | PF-006 + PF-007 |
| 50-ORIGINAL-FEATURES Cat 1 (10) | Debugging & Diagnostics | PF-038 (DevTools panel) |
| 50-ORIGINAL-FEATURES Cat 2 (8) | Design Intelligence | PF-021 (Roy AI) |
| 50-ORIGINAL-FEATURES Cat 3 (8) | Performance | PF-037 (Perf Framework) |
| 50-ORIGINAL-FEATURES Cat 4 (6) | Accessibility | PF-004 + PF-023 |
| 50-ORIGINAL-FEATURES Cat 5 (6) | Multi-Brand & Theming | PF-019 + PF-026 + PF-030 |
| 50-ORIGINAL-FEATURES Cat 6 (6) | Developer Tools | PF-036 + PF-038 |
| 50-ORIGINAL-FEATURES Cat 7 (6) | Animation Orchestration | PF-017 + PF-027 |
| 50-ORIGINAL-FEATURES Cat 8 (6) | Build & Distribution | PF-016 + PF-035 |
| PLATFORM-VISION §1.1-1.13 | 12 products | PF-016 → PF-031 + PF-039/040 |
| PLATFORM-VISION §4.1-4.10 | 10 unique features | PF-021 + PF-022 + PF-038 |
| PLATFORM-VISION §6.1-6.4 | GTM phases | PF-010 + PF-014 + PF-019 + PF-045 |
| ROYCSS-V2-BLUEPRINT §1-16 | V2 spec | PF-032 → PF-047 |
| LABS-26 RoyLang | RoyLang | PF-033 |
| LABS-27 Research | Research directions | PF-046 |
| LABS-28 Delete Half | Cut proposal | PF-047 |
| LABS-29 Apple HIG | Design refinements | PF-014 + PF-026 + PF-038 |
| LABS-30 1M Users | Scalability plan | PF-004 + PF-005 + PF-006 + PF-013 + PF-042 |
| LABS-31 Eliminate Boilerplate | Intent abstractions | PF-033 |
| LABS-32 AI Code Review | AI-friendly redesign | PF-021 + PF-036 + PF-041 |
| LABS-33 Performance Lab | Performance budget | PF-006 + PF-037 |
| LABS-34 Framework Killer | Lock-in prevention | PF-015 + PF-030 |
| LABS-35 10-Year Architecture | Stable core / API / plugins | PF-005 + PF-013 + PF-034 + PF-042 + PF-044 |
| LABS-36 Impossible Question | CSS psychology redesign | PF-033 + PF-035 + PF-036 |
| ENTERPRISE-REVIEW R1-R3 | Critical risks | PF-005 |
| ENTERPRISE-REVIEW R4-R9 | High/medium risks | PF-002 + PF-006 + PF-031 |
| ENTERPRISE-REVIEW §5 | Adoption checklist gates | PF-004 + PF-005 + PF-006 + PF-013 |
| COMPETITIVE-ANALYSIS R1-R15 | Recommendations | PF-014 + PF-016 + PF-018 + PF-031 + PF-038 + PF-045 |
| FERRUM-MIGRATION-ANALYSIS | Migration | PF-012 + PF-015 (Ferrum P0/P1 effect batches) |
| docs/reports/SECURITY.md | Security policy | DONE (existing) |
| docs/reports/DEVELOPMENT.md | Dev guide | DONE (existing) |
| docs/reports/AUDIT-REPORT.md | Audit (overstated) | superseded |
| docs/reports/FINAL-VERIFICATION.md | Final verify (overstated) | superseded |
| docs/reports/RESPONSIVE-AUDIT.md | Responsive audit | DONE (no issues) |
| docs/reports/ARCHITECTURE.md | Architecture overview | DONE (living doc) |
| docs/reports/API_KEYS_REQUIRED.md | API keys table | PF-003 |

---

## Notes on stale / superseded sources

The following source documents are **partially stale or superseded** by
this consolidated list:

1. `docs/plans/BACKEND-COMPLETION-REQUIREMENTS.md` — claims "62 product
   cards don't fetch from backend" (stale: 37 now use `useBackendData`
   per FIX-F1-F2) and "current schema has 4 models" (stale: 45 now
   exist per AUDIT-1). Replaced by this file.

2. `docs/reports/AUDIT-REPORT.md` and `docs/reports/FINAL-VERIFICATION.md`
   — claim "all 68 backend modules with real implementations"
   (overstated per AUDIT-1: 6 modules are intentional static
   snapshots, 4 are catalog/scaffold mocks, 12 use mock fallback).
   Replaced by this file.

3. `docs/PENDING-FEATURES-AUDIT.md` (889 lines) — the master
   cross-reference inventory. Superseded by this file (the actionable
   backlog). To be removed from the repo once this file is committed.

4. `docs/50-ORIGINAL-FEATURES.md`, `docs/PLATFORM-VISION.md`,
   `docs/ROYCSS-V2-BLUEPRINT.md`, `docs/LABS-26-36*.md`,
   `docs/ENTERPRISE-REVIEW.md`, `docs/COMPETITIVE-ANALYSIS.md`,
   `docs/FERRUM-MIGRATION-ANALYSIS.md` — strategic / design docs.
   Their cross-reference implementations are tracked in this file;
   the strategic docs themselves remain useful as design context (not
   pending-features trackers).

5. `docs/ROYCSS_BACKEND_TODO.md`, `docs/ROYCSS_BACKEND_ARCHITECTURE.md`,
   `docs/ROYCSS_MIGRATION_GUIDE.md`, `docs/ROYCSS_DATABASE_ARCHITECTURE.md`,
   `docs/ROYCSS_API_SPECIFICATION.md`, `docs/ROYCSS_SECURITY_REPORT.md`,
   `docs/ROYCSS_PERFORMANCE_REPORT.md`, `docs/ROYCSS_OBSERVABILITY.md`,
   `docs/ROYCSS_DEPLOYMENT.md`, `docs/ROYCSS_TEST_REPORT.md` —
   companion reports. Their TODO items are tracked in this file; the
   companion reports themselves remain useful as architectural context
   (not pending-features trackers).

---

**End of consolidated pending-features list.** Report version: 2.0.
Generated by task PENDING-AUDIT.
