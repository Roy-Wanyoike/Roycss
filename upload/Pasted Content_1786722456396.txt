Let me analyze both redesign screenshots thoroughly.
Now I have a complete picture of both redesign screenshots. Let me cross-reference with all previous audit findings and synthesize the comprehensive build prompt.
Here is the **complete multi-agent rebuild prompt** for the RoyCSS website redesign. Copy everything below the line:

---

# RoyCSS Website — Complete Rebuild Prompt

## Context & Reference

You are rebuilding the RoyCSS website (https://roycss.space-z.ai/) from a Next.js App Router + Tailwind CSS 4 + TypeScript codebase. Two design reference screenshots have been provided showing the new visual direction. This prompt is the single source of truth. Follow it completely. Do not skip any section. Do not make removals unless explicitly instructed. Do not lose any existing features, effects data, platform products, or functionality during the rebuild.

---

## PART 1 — CRITICAL BUG FIXES (Fix First, Before Any Visual Work)

These are confirmed bugs from QA audit. Fix every single one before starting visual work.

### BUG-001: Header `overflow: hidden` [BLOCKER]
- **Problem**: The `<header>` element has `overflow: hidden` and a height of ~874px. This clips the entire page, making content below y=26,661px unreachable. The Platform section and all content below it is invisible and unreachable.
- **Fix**: Remove `overflow-hidden` from the `<header>`. If overflow clipping is needed for a visual effect (gradient bleed), apply it to an inner `<div>` instead, never the header itself.
- **Verify**: After fix, the page must scroll freely to the very bottom. All sections including Platform, FAQ, and Footer must be reachable.

### BUG-002: Body `overflow: hidden` on Mobile [BLOCKER]
- **Problem**: At 375px viewport, `<body>` gets `overflow: hidden`, preventing all mobile scrolling.
- **Fix**: Only set `overflow: hidden` on body when a modal/dialog is open. Remove any CSS rule or JS that sets it unconditionally on small viewports.
- **Verify**: At 375px, the page must scroll naturally from top to bottom with touch/swipe.

### BUG-003: H1 Missing Space
- **Problem**: H1 renders as "Build BeautifulFrontend Interfaces" — missing space between "Beautiful" and "Frontend".
- **Fix**: In the JSX, ensure proper spacing. The new H1 from the redesign is: Line 1: "Build **Beautiful**." Line 2: "Ship Faster." — implement this exactly.
- **Verify**: No concatenated words anywhere in headings.

### BUG-004: HTML Theme Conflict
- **Problem**: `<html class="dark" style="color-scheme: light;">` — the inline style contradicts the class.
- **Fix**: Remove the inline `style="color-scheme: light;"`. Let the CSS custom properties on `:root` and `[data-theme="dark"]` handle the color scheme. Set `color-scheme: dark dark` on `[data-theme="dark"]` in CSS.
- **Verify**: No inline styles on `<html>`. Theme toggle works correctly.

### BUG-005: Wrong Canonical/OG Domains
- **Problem**: `<link rel="canonical">`, `og:image`, and related meta tags point to `roycss.com` instead of `roycss.space-z.ai`.
- **Fix**: Update all meta tag URLs to use `https://roycss.space-z.ai` as the canonical domain.
- **Verify**: View page source — all URLs reference roycss.space-z.ai.

### BUG-006: Missing Meta Tags
- **Problem**: Missing `og:type`, `og:url`, `og:locale`, `twitter:card`, `twitter:image`.
- **Fix**: Add complete Open Graph and Twitter Card meta tags:
  ```
  og:type = "website"
  og:url = "https://roycss.space-z.ai"
  og:locale = "en_US"
  twitter:card = "summary_large_image"
  twitter:image = "https://roycss.space-z.ai/og-image.png" (create this asset)
  ```

### BUG-007: Ghost Tutorial Dialog
- **Problem`: A `[role="dialog"]` element persists in the DOM after dismissal with 9 interactive buttons still accessible to screen readers.
- **Fix**: Unmount the dialog entirely when dismissed (conditional render, not CSS display:none). Set a `localStorage` flag so it doesn't reappear on reload.
- **Verify**: After dismissing, `document.querySelectorAll('[role="dialog"]')` returns empty.

### BUG-008: Platform Modal aria-label
- **Problem**: aria-label renders as "Pro Data GridReadyPro" — missing spaces between badge and product name.
- **Fix**: Add proper separators: `aria-label={\`${product.badge} ${product.name}\`}` — ensure spaces between all concatenated strings.
- **Verify**: Every modal has a readable, properly spaced aria-label.

### BUG-009: Effect Count Inconsistency
- **Problem**: Site claims "1,749 effects" in some places and "1,569" in others.
- **Fix**: Determine the authoritative count from the actual effects data array length. Use that single source of truth everywhere — hero stats, meta descriptions, sitemap, etc.

### BUG-010: manifest.json & robots.txt
- **Problem**: manifest.json has stale description, single icon size, wrong domain. robots.txt points sitemap to roycss.com.
- **Fix**: 
  - manifest.json: Update description, add 192x192 and 512x512 icons, set `start_url` to `/`.
  - robots.txt: Change sitemap URL to `https://roycss.space-z.ai/sitemap.xml`.

### BUG-011: 171 Undersized Mobile Touch Targets
- **Problem**: 171 interactive elements are smaller than 36px at 375px viewport.
- **Fix**: All clickable elements must be minimum 44×44px touch target. Use `min-h-11 min-w-11` (44px) on buttons and icon buttons. For horizontally scrolling chip filters, ensure each chip has adequate padding.

---

## PART 2 — ARCHITECTURE: Multi-Page Routing

The current site is a single 35,788px page. Rebuild it as a proper multi-page Next.js App Router application.

### Route Map
```
/                          → Homepage (redesign as shown in reference screenshots)
/effects                   → Effects browser with left sidebar + grid
/effects/[slug]            → Individual effect detail/modal page
/platform                  → Platform products with left sidebar + grid
/platform/[slug]           → Individual product detail page
/docs                      → Documentation home
/docs/getting-started      → Getting started guide
/docs/installation         → Installation guide
/docs/usage                → Usage guide
/docs/api                  → API reference
/docs/migration            → Migration guide
/docs/changelog            → Changelog
/recipes                   → Recipes listing
/recipes/[slug]            → Individual recipe
/pricing                   → Pricing page
/resources                 → Resources hub (blog posts, videos, community)
/favorites                 → User's favorited effects (localStorage)
```

### Layout Hierarchy
```
app/
  layout.tsx                → Root layout: html, body, providers, Toaster
  page.tsx                  → Homepage (redesign)
  effects/
    layout.tsx              → Sidebar + content layout (Pattern B from audit)
    page.tsx                → Effects grid with filters
    [slug]/page.tsx         → Effect detail
  platform/
    layout.tsx              → Sidebar + content layout
    page.tsx                → Platform products grid
    [slug]/page.tsx         → Product detail
  docs/
    layout.tsx              → Docs sidebar + TOC layout (Pattern C)
    page.tsx                → Docs home
    getting-started/page.tsx
    installation/page.tsx
    usage/page.tsx
    api/page.tsx
    migration/page.tsx
    changelog/page.tsx
  recipes/
    layout.tsx
    page.tsx
    [slug]/page.tsx
  pricing/page.tsx
  resources/page.tsx
  favorites/page.tsx
```

---

## PART 3 — NAVBAR (Exact Specification from Redesign)

### Structure
The navbar is sticky (`sticky top-0 z-50`), full-width, with glassmorphism (`backdrop-blur-md bg-background/80 border-b border-border/50`).

### Left Section
```
[Logo Icon: rounded-xl, </> symbol, teal/emerald gradient bg] 
[RoyCSS text: font-bold, text-lg, text-foreground]
[v1.0 badge: text-xs, text-emerald-400, ml-1]
```
The logo+text is a single `<Link href="/">` wrapping both. The logo icon is a 36×36px rounded div with animated gradient background (`linear-gradient(135deg, #10b981, #14b8a6, #06b6d4, #8b5cf6, #10b981)` with `background-size: 300% 300%` animation).

### Center Navigation Links (desktop ≥1024px, `hidden lg:flex`)
These are `<Link>` elements, NOT buttons. Each has `px-4 py-2.5 rounded-xl text-sm font-medium text-muted-foreground hover:text-foreground hover:bg-muted/60`:

| Label | Type | Route/Action |
|-------|------|-------------|
| Get Started | Link | `/docs/getting-started` |
| Explore | Dropdown (Radix) | Mega-menu or flyout with: All Effects, Categories, Trending, New, Random |
| Platform | Dropdown (Radix) | Flyout with: All Products, By Tier (Free/Pro/Team/Enterprise), Compare |
| Docs | Link | `/docs` |
| Resources | Dropdown (Radix) | Flyout with: Blog, Videos, Community, Changelog |
| Pricing | Link | `/pricing` |

Dropdown triggers show a `<ChevronDown>` icon (lucide, size-3) with `transition-transform duration-200` rotating on open.

### Right Section (Utility Cluster)
Right-aligned, `flex items-center gap-2`:

1. **Search Trigger** — `size-11 rounded-xl glass` button with `<Search>` lucide icon. On click, opens a command palette dialog (`⌘K`). This is the ONLY search — remove any inline search inputs from the navbar.

2. **GitHub Link** — `<a href="https://github.com/Roy-Wanyoike/roycss" target="_blank" rel="noopener noreferrer">`, `size-11 rounded-xl glass`, `<Github>` lucide icon. Hidden below `lg`.

3. **Theme Toggle** — `size-11 rounded-xl glass` button, `<Sun>` / `<Moon>` lucide icon, toggles `[data-theme="dark"]` on `<html>`.

4. **Favorites** — `size-11 rounded-xl glass` button, `<Heart>` lucide icon, links to `/favorites`. Shows a small badge count if user has favorites in localStorage.

5. **Sponsor CTA** — `px-5 py-2.5 rounded-full bg-emerald-500 text-white text-sm font-semibold hover:bg-emerald-600 transition-colors`. Links to GitHub Sponsors. Contains `<Heart>` icon + "Sponsor" text.

### Mobile Behavior (<1024px)
- Center nav links: `hidden lg:flex` — completely hidden.
- A hamburger menu button (`lg:hidden`, `size-11 rounded-xl glass`, `<Menu>` icon) opens a full-screen slide-over menu with all nav links stacked vertically, each as a `<Link>`.
- Optionally add a **bottom tab bar** (fixed `bottom-0 left-0 right-0 z-50`) with 5 items: Home, Effects, Platform, Docs, More (⋯).

### DO NOT Include in Navbar
- DevTools wrench button (move to `/docs/devtools` or settings)
- Layout/grid toggle icon (remove — not a nav concern)
- Profile/user icon (add later when auth exists)

---

## PART 4 — HOMEPAGE (Exact Specification from Redesign Screenshots)

### Overall Page Structure
```
<Navbar />
<HeroSection />          ← Split layout: Left marketing + Right Roy Studio
<FeatureGrid />          ← 5-column cards
<SocialProof />          ← Logo marquee
<StatsBar />             ← 6-column metrics
<Footer />
```

The page uses the site's dark theme by default (`data-theme="dark"` on html). Background is deep space black (`#030712` to `#0f172a`) with subtle radial gradient glows (purple/blue, `opacity: 0.1`, `blur: 3xl`) positioned behind content for depth.

### Container Width
- **Homepage sections**: `max-w-[1400px] mx-auto px-6 lg:px-8` — significantly wider than the old 1280px.
- **Full-bleed sections** (stats bar, social proof): No max-width, full viewport width with internal padding.

### 4A. Hero Section — Two-Column Split Layout

**Layout**: `grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center`, `min-h-[calc(100vh-80px)]` (full viewport minus nav height).

#### Left Column (Marketing Copy)

**Eyebrow Badge**:
```html
<div class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-emerald-500/30 bg-emerald-500/5 text-sm text-emerald-400 mb-6">
  <Sparkles size={14} />
  AI-Native Frontend Engineering Platform
</div>
```

**Headline H1**:
```html
<h1 class="text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-extrabold tracking-tight leading-[1.1]">
  Build{' '}
  <span class="bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-500 bg-clip-text text-transparent">
    Beautiful
  </span>
  .<br />
  Ship Faster.
</h1>
```

**Subheadline**:
```html
<p class="mt-6 text-lg sm:text-xl text-muted-foreground leading-relaxed max-w-xl">
  1,749 CSS effects, 62 platform products, and 64 developer tools — all pure CSS with zero JavaScript runtime. AI-assisted, OKLCH color system, production-ready.
</p>
```

**CTA Group** (flex row, gap-4, mt-8):
```html
<!-- npm install button -->
<button class="flex items-center gap-3 px-5 py-3 rounded-xl bg-card border border-border/50 text-sm font-mono hover:bg-muted/50 transition-colors">
  <span class="text-muted-foreground">$</span>
  <span>npm install roycss</span>
  <Copy size={14} class="text-muted-foreground ml-2" />
</button>

<!-- Primary CTA -->
<Link href="/platform" class="flex items-center gap-2 px-6 py-3 rounded-xl bg-emerald-500 text-white font-semibold hover:bg-emerald-600 transition-colors">
  Explore the Platform
  <ArrowRight size={16} />
</Link>
```

**Trust Indicators** (flex row, gap-6, mt-6):
```html
<div class="flex flex-wrap items-center gap-4 text-sm text-muted-foreground">
  <span class="flex items-center gap-1.5"><Check size={14} class="text-emerald-400" /> OKLCH Colors</span>
  <span class="flex items-center gap-1.5"><Check size={14} class="text-emerald-400" /> Zero JS Runtime</span>
  <span class="flex items-center gap-1.5"><Check size={14} class="text-emerald-400" /> MIT Licensed</span>
</div>
```

**Stats Row** (4 cards, grid grid-cols-2 sm:grid-cols-4 gap-4, mt-10):
Each stat card: `p-4 rounded-xl bg-card/50 border border-border/30`
```html
<!-- Example stat card -->
<div class="p-4 rounded-xl bg-card/50 border border-border/30">
  <Sparkles size={18} class="text-emerald-400 mb-2" />
  <div class="text-2xl font-bold text-foreground">1,749</div>
  <div class="text-sm text-muted-foreground">Effects</div>
</div>
```
Stats: 1,749 Effects (Sparkles icon), 29 Categories (Layers icon), 62 Products (Box icon), ~22,000+ Lines (Zap icon).

#### Right Column — Roy Studio Panel

This is the **most important visual element** of the redesign. It's an embedded interactive component browser that demonstrates RoyCSS capabilities directly on the homepage.

**Outer Container**:
```html
<div class="relative rounded-2xl border border-border/50 bg-[#0a0f1c] overflow-hidden shadow-2xl shadow-black/50">
  <!-- Subtle gradient glow behind the panel -->
  <div class="absolute -inset-4 bg-gradient-to-r from-purple-500/10 via-blue-500/10 to-cyan-500/10 blur-3xl -z-10" />
```

**Panel Header**:
```html
<div class="flex items-center justify-between px-4 py-3 border-b border-border/30">
  <div class="flex items-center gap-2">
    <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse" />
    <span class="text-sm font-semibold text-foreground">Roy Studio</span>
    <ChevronDown size={14} class="text-muted-foreground" />
  </div>
  <div class="flex items-center gap-2">
    <!-- Search inside studio -->
    <div class="flex items-center gap-2 px-3 py-1.5 rounded-lg bg-white/5 text-sm text-muted-foreground">
      <Search size={12} />
      <span>Search components...</span>
      <kbd class="ml-4 text-xs bg-white/10 px-1.5 py-0.5 rounded">⌘K</kbd>
    </div>
    <Link href="/platform" class="flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-emerald-500/30 text-xs text-emerald-400 hover:bg-emerald-500/10 transition-colors">
      Open in Studio <ExternalLink size={12} />
    </Link>
  </div>
</div>
```

**Panel Body — Sidebar + Content** (`flex`, `h-[480px]`):

**Left Sidebar** (w-48, border-right, `flex flex-col`):
```html
<aside class="w-48 border-r border-border/30 flex flex-col py-2 text-sm">
  {sidebarItems.map(item => (
    <button key={item.label} class={cn(
      "flex items-center gap-2.5 px-4 py-2 text-left transition-colors",
      item.active 
        ? "text-emerald-400 bg-emerald-500/10 border-l-2 border-emerald-400" 
        : "text-muted-foreground hover:text-foreground hover:bg-white/5"
    )}>
      <item.icon size={16} />
      {item.label}
    </button>
  ))}
  <!-- Theme switcher at bottom -->
  <div className="mt-auto px-4 py-2 border-t border-border/30">
    <button className="flex items-center gap-2 text-sm text-muted-foreground">
      <Moon size={14} /> Dark
    </button>
  </div>
</aside>
```

Sidebar items: Explorer (active), Components, Effects, Patterns, Templates, Tokens, Themes, Icons, Motion, Docs.

**Main Content Area** (flex-1, overflow-y-auto, p-4):
Contains two sections stacked vertically:

**Section 1: Popular Components** — 2×2 grid, `grid grid-cols-2 gap-3`:
Each card: `rounded-lg border border-border/30 bg-white/[0.02] p-3 hover:bg-white/[0.05] transition-colors`
- Card shows a mini preview (actual rendered CSS component or placeholder visual), title, subtitle.
- Components: Button ("Multiple variants"), Card ("Adaptive layouts"), Modal ("Dialog system"), Navbar ("Responsive navigation").

**Section 2: Trending Effects** — 2×2 grid, same card style:
- Aurora (gradient blob visual), Magnetic (circle interaction), Glow Grid (dot matrix), Text Reveal (gradient text).
- Each card shows a live CSS preview or animated placeholder.

Each section has a header: `"POPULAR COMPONENTS"` / `"TRENDING EFFECTS"` in uppercase `text-xs font-medium text-muted-foreground tracking-wider mb-3`, with a `"View all >"` link in `text-emerald-400 text-xs` aligned right.

### 4B. Feature Grid Section

**Container**: `py-24 max-w-[1400px] mx-auto px-6 lg:px-8`

**Section Header**: Centered, `text-center mb-16`
```html
<h2 class="text-3xl sm:text-4xl font-bold text-foreground">Everything you need to build modern interfaces</h2>
<p class="mt-4 text-lg text-muted-foreground max-w-2xl mx-auto">From design tokens to production deployment, RoyCSS provides a complete engineering platform.</p>
```

**Grid**: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-6`

5 Feature Cards, each with:
```html
<div class="p-6 rounded-2xl border border-border/30 bg-card/30 hover:bg-card/50 hover:border-border/50 transition-all group">
  <div class="w-10 h-10 rounded-xl bg-{color}-500/10 flex items-center justify-center mb-4">
    <Icon size={20} className="text-{color}-400" />
  </div>
  <h3 class="font-semibold text-foreground mb-2">{Title}</h3>
  <p class="text-sm text-muted-foreground leading-relaxed">{Description (2 lines)}</p>
</div>
```

| # | Title | Icon | Color | Description |
|---|-------|------|-------|-------------|
| 1 | Design & Build | Sparkles | emerald | Visual studio, design tokens, component library with live preview and OKLCH color system. |
| 2 | AI Assistance | Brain | purple | AI-powered code generation, smart suggestions, and automated effect customization. |
| 3 | Developer Tools | Wrench | blue | CLI, DevTools integration, linters, and automated workflow tooling. |
| 4 | Quality & Testing | Shield | rose | Accessibility auditing, performance monitoring, cross-browser testing built in. |
| 5 | Deploy & Monitor | Monitor | emerald | Production-ready outputs, bundle analysis, and deployment confidence metrics. |

### 4C. Social Proof Section

**Full-bleed**: No max-width container, use `py-16 border-y border-border/20`

**Content**: `max-w-[1400px] mx-auto px-6 lg:px-8 text-center`
- Label: `<p class="text-sm text-muted-foreground mb-8">Trusted by developers and teams worldwide</p>`
- Logo grid: `flex flex-wrap items-center justify-center gap-8 sm:gap-12` — 8 logos (Vercel, Netlify, GitHub, Linear, Docker, GitLab, Cloudflare, Figma) rendered as grayscale SVGs with `opacity-40 hover:opacity-100 transition-opacity`.

### 4D. Stats Bar Section

**Full-bleed**: `py-12 bg-gradient-to-r from-emerald-500/5 via-blue-500/5 to-purple-500/5 border-t border-border/20`

**Grid**: `grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-8 max-w-[1400px] mx-auto px-6 lg:px-8`

Each stat: `text-center`
```html
<div className="text-center">
  <Icon size={20} className="mx-auto mb-2 text-{color}-400" />
  <div className="text-2xl font-bold text-foreground">{value}</div>
  <div className="text-sm text-muted-foreground">{label}</div>
</div>
```

| Label | Value | Icon | Color |
|-------|-------|------|-------|
| 50K+ Developers | 50K+ | Users | emerald |
| 120K+ Projects | 120K+ | FolderOpen | emerald |
| 180+ Countries | 180+ | Globe | purple |
| 99.9% Uptime | 99.9% | Clock | cyan |
| 24/7 Community | 24/7 | MessageCircle | blue |
| 4.9 Satisfaction | 4.9 | Star | rose |

---

## PART 5 — INNER PAGE LAYOUTS

### 5A. Effects Page (`/effects`) — Sidebar + Grid

**Layout**: `flex min-h-[calc(100vh-64px)]`
- **Left Sidebar** (w-60, sticky top-16, h-[calc(100vh-64px)], overflow-y-auto, border-r):
  - Category list: 20 categories, each with count badge, collapsible sections
  - Quick filters: Framework (All/React/Vue/Angular/Svelte/Next.js), Badge (Ready/Pro/Beta/Free/Enterprise/Cloud)
  - "Random Effect" button at bottom
- **Main Content** (flex-1, p-6):
  - Header: "Explore Effects" title + sort dropdown + view toggle (grid/list) + active filter tags
  - Effect grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4`
  - Each effect card: thumbnail preview, name, category tag, badge, hover opens quick detail panel
- **Right Panel** (w-56, hidden xl:block, sticky top-16):
  - Active filters summary
  - Effect count ("Showing X of 1,749")
  - Color picker for live preview tinting
  - Recently viewed effects list

### 5B. Platform Page (`/platform`) — Same sidebar pattern

Left sidebar has tier groups (Free, Pro, Team, Enterprise) + category list.
Main content: product cards with screenshot previews, feature lists, pricing.

### 5C. Docs Pages — VitePress-style Layout

- **Left Sidebar**: Collapsible navigation tree (Getting Started, API Reference, Recipes, Migration, Changelog)
- **Main Content**: Markdown-rendered documentation, `max-w-3xl` for prose readability
- **Right Aside**: Auto-generated Table of Contents from headings, sticky

---

## PART 6 — DESIGN SYSTEM TOKENS

### Colors (OKLCH-based)
```
--background:       oklch(0.1 0.01 260)       /* #0a0f1c deep navy */
--foreground:       oklch(0.98 0 0)            /* #fafafa near-white */
--card:             oklch(0.15 0.01 260)       /* #111827 surface */
--card-foreground:  oklch(0.95 0 0)            /* #f0f0f0 */
--muted:            oklch(0.2 0.01 260)        /* #1e293b */
--muted-foreground: oklch(0.55 0.01 260)       /* #94a3b8 */
--border:           oklch(0.25 0.01 260)       /* #334155 */
--primary:          oklch(0.7 0.17 162)        /* #10b981 emerald */
--primary-foreground: oklch(0.98 0 0)
--accent:           oklch(0.7 0.15 200)        /* #06b6d4 cyan */
--secondary:        oklch(0.65 0.2 295)        /* #a855f7 purple */
```

### Typography
- **Font Family**: Inter for body, JetBrains Mono for code blocks
- **Scale**: 14/16/18/20/24/30/36/48/60/72px using Tailwind's default scale
- **Weights**: 400 (body), 500 (labels), 600 (subheadings), 700 (headings), 800 (hero)

### Spacing (8px grid)
- Section padding: `py-20 lg:py-24`
- Container padding: `px-6 lg:px-8`
- Card padding: `p-6`
- Gap system: `gap-4` (cards), `gap-6` (sections), `gap-8` (major), `gap-12` (hero columns)

### Border Radius
- Buttons/pills: `rounded-full` or `rounded-xl`
- Cards: `rounded-xl` or `rounded-2xl`
- Inputs: `rounded-lg` or `rounded-xl`
- Badges: `rounded-full`

### Glass Effect (`.glass`)
```css
.glass {
  background: oklch(0.15 0.01 260 / 0.5);
  backdrop-filter: blur(12px);
  border: 1px solid oklch(0.3 0.01 260 / 0.5);
}
```

---

## PART 7 — RESPONSIVE SPECIFICATIONS

### Breakpoints
| Breakpoint | Width | Layout Changes |
|-----------|-------|----------------|
| Mobile S | 375px | Single column, bottom tab nav, stacked cards, Roy Studio hidden or simplified |
| Mobile L | 424px | Same, slightly more horizontal space |
| Tablet | 768px | Hero starts 2-column, 2-col grids, sidebar collapses to sheet |
| Desktop S | 1024px | Full navbar visible, 3-col grids, sidebar visible |
| Desktop M | 1366px | 4-col effect grids, all panels visible |
| Desktop L | 1920px | 5-col grids, generous spacing, Roy Studio fully expanded |

### Mobile (< 768px) Specific Rules
- **No `overflow: hidden` on body** (BUG-002)
- **Bottom tab bar** (fixed, `h-16`, glass bg): Home, Effects, Platform, Docs, More
- **Hero**: Stack columns — marketing copy on top, Roy Studio panel below (simplified, single-column preview)
- **Feature grid**: 1 column, stacked cards
- **Stats**: 2-column grid
- **All touch targets**: Minimum 44×44px
- **Chip filters**: Horizontal scroll with `overflow-x-auto`, `scroll-snap-type: x mandatory`

### Tablet (768px–1023px)
- **Hero**: 2-column grid but narrower studio panel
- **Sidebar**: Hidden, accessible via hamburger or slide-over sheet
- **Feature grid**: 2 columns
- **Effect grid**: 2-3 columns

---

## PART 8 — COMPONENTS TO BUILD

### Navigation Components
1. `Navbar` — As specified in Part 3
2. `MobileBottomTabBar` — 5-item fixed bottom nav
3. `MobileMenu` — Full-screen slide-over
4. `Sidebar` — Shared sidebar component for effects/platform/docs pages
5. `TableOfContents` — Auto-generated from page headings
6. `Breadcrumb` — For inner pages

### Layout Components
1. `HomepageLayout` — Full-width with sections
2. `SidebarLayout` — Sidebar + content + optional right panel
3. `DocsLayout` — Sidebar + content + TOC
4. `Container` — `max-w-[1400px] mx-auto px-6 lg:px-8`

### Feature Components
1. `RoyStudio` — The embedded IDE component browser (hero right panel)
2. `CommandPalette` — ⌘K search dialog (Cmdk/Radix)
3. `EffectCard` — Thumbnail + name + tags + hover actions
4. `EffectModal` — Full detail view with live preview, 6 framework tabs, star rating, CodePen integration
5. `PlatformProductCard` — Product preview with screenshot, features, tier badge
6. `ProductModal` — Full product detail with demo widget
7. `ThemeToggle` — Sun/Moon switch
8. `ColorPicker` — OKLCH color selection widget
9. `CodeBlock` — Syntax-highlighted code with copy button and framework tabs
10. `StatCard` — Icon + number + label
11. `FeatureCard` — Icon + title + description
12. `LogoMarquee` — Infinite scroll logo carousel
13. `TrustBadges` — Checkmark items
14. `SearchFilterBar` — Active filters + sort + view toggle

### Utility Components
1. `Toaster` — Sonner/react-hot-toast for notifications
2. `Dialog` — Accessible modal (Radix)
3. `DropdownMenu` — Radix dropdown
4. `Sheet` — Slide-over panel (mobile sidebar)
5. `Tabs` — Framework code tabs
6. `Badge` — Tier/badge component (Ready/Pro/Beta/Free/Enterprise/Cloud)
7. `Skeleton` — Loading states

---

## PART 9 — DATA STRUCTURES

### Effect Interface
```typescript
interface Effect {
  slug: string;
  name: string;
  category: string;          // One of 20 categories
  description: string;
  tags: string[];
  css: string;               // The actual CSS code
  author?: string;
  version: string;
  lastUpdated: string;       // ISO date
  size: string;              // e.g. "2.4kB"
  browserSupport: Record<string, 'full' | 'partial' | 'none'>;
  performanceRating: 'low' | 'medium' | 'high';
  respectsReducedMotion: boolean;
  relatedEffects: string[];  // Slugs
  usedInRecipes: string[];   // Recipe slugs
  preview?: {
    light: string;           // CSS class or inline style for light preview
    dark: string;
  };
}
```

### Platform Product Interface
```typescript
interface PlatformProduct {
  slug: string;
  name: string;
  description: string;
  longDescription: string;
  tier: 'free' | 'pro' | 'team' | 'enterprise';
  badge: 'Ready' | 'Beta' | 'Free' | 'Pro' | 'Enterprise' | 'Cloud';
  category: string;
  features: string[];
  screenshot?: string;
  demoWidget?: string;
  pricing?: { monthly: number; annual?: number };
  docsUrl?: string;
  relatedProducts: string[];
}
```

---

## PART 10 — PERFORMANCE & SEO

### Performance
- All images: Next.js `<Image>` with proper `sizes` and `priority` for above-fold
- Fonts: `next/font` with `display: swap`, preload Inter + JetBrains Mono
- No layout shift: All containers have fixed/aspect-ratio dimensions
- Code splitting: Each route is a separate chunk
- Roy Studio panel: Lazy-loaded with `next/dynamic` and `ssr: false`
- Effect previews: Use CSS-only where possible, lazy-load heavy animations

### SEO
- Each page has proper `<title>`, `<meta name="description">`, Open Graph tags
- Structured data (JSON-LD) for: SoftwareApplication, WebApplication, FAQPage
- Sitemap.xml generated via `next-sitemap` covering all routes
- Canonical URLs all point to `https://roycss.space-z.ai`

---

## PART 11 — ACCESSIBILITY

- All interactive elements have visible focus rings (`focus-visible:ring-2 ring-emerald-500 ring-offset-2 ring-offset-background`)
- Color contrast: All text meets WCAG AA (4.5:1 for normal, 3:1 for large)
- Keyboard navigation: Tab order follows visual order, all dropdowns keyboard-operable
- `prefers-reduced-motion`: Disable all animations when user prefers reduced motion
- ARIA: All modals use `role="dialog"`, `aria-modal="true"`, `aria-label`
- Skip-to-content link as first focusable element
- No `alert()`, `prompt()`, or `confirm()` — use Toaster/Dialog

---

## PART 12 — MULTI-AGENT WORK STREAMS

Execute these streams in parallel. Each stream is independent and can be worked on simultaneously by separate agents.

### Stream 1: Architecture & Routing (Foundation)
- Set up Next.js App Router file structure (Part 2)
- Create root layout with theme provider, Toaster, metadata
- Create all route layouts (sidebar, docs, etc.)
- Implement theme system with `[data-theme]` attribute
- Fix BUG-004 (theme conflict)
- Verify all routes render without errors

### Stream 2: Critical Bug Fixes
- Fix all 11 bugs listed in Part 1
- Verify each fix with specific assertions
- Run through the checklist: no overflow:hidden on header/body, proper aria-labels, correct domains, no ghost dialogs

### Stream 3: Navbar Component
- Build `Navbar` per Part 3 specification
- Build `MobileMenu` slide-over
- Build `MobileBottomTabBar`
- Build `CommandPalette` (⌘K search)
- Implement dropdown menus with Radix
- Test at all breakpoints: 375, 768, 1024, 1366, 1920

### Stream 4: Homepage — Hero Section
- Build the 2-column split hero (Part 4A)
- Build the `RoyStudio` embedded component browser
- Implement animated gradient background
- Build stat cards, trust badges, CTA group
- Ensure proper responsive stacking on mobile

### Stream 5: Homepage — Lower Sections
- Build Feature Grid (Part 4B) — 5-column cards
- Build Social Proof logo marquee (Part 4C)
- Build Stats Bar (Part 4D) — 6-column metrics
- Build Footer component
- All full-bleed where specified

### Stream 6: Inner Pages — Effects & Platform
- Build `SidebarLayout` component
- Build `/effects` page with category sidebar, filter system, effect grid
- Build `EffectCard` and `EffectModal` (6 framework tabs, live preview, star rating, CodePen)
- Build `/platform` page with tier-filtered sidebar, product grid
- Build `PlatformProductCard` and `ProductModal`

### Stream 7: Inner Pages — Docs, Pricing, Resources, Favorites
- Build `DocsLayout` with sidebar + TOC
- Build all doc pages (getting-started, installation, usage, api, migration, changelog)
- Build `/pricing` page with tier comparison table
- Build `/resources` hub page
- Build `/favorites` page (localStorage-backed)

### Stream 8: Design System, Responsive QA & Polish
- Implement all design tokens (Part 6)
- Build shared components (Part 8)
- Responsive QA at 375, 424, 768, 1024, 1366, 1920
- Verify all touch targets ≥ 44px
- Verify focus rings, ARIA, reduced-motion
- Lighthouse audit: Performance >90, Accessibility >95, SEO >95
- Cross-browser visual check

---

## ACCEPTANCE CRITERIA — All Streams Complete When:

- [ ] All 11 critical bugs are fixed and verified
- [ ] Multi-page routing works — every route in Part 2 renders correctly
- [ ] Navbar matches Part 3 exactly — links, icons, mobile menu, ⌘K search
- [ ] Homepage hero is a 2-column split with Roy Studio panel on the right
- [ ] Roy Studio panel has working sidebar with 10 nav items, search, component/effect preview grids
- [ ] Feature grid is 5 columns on desktop, responsive on mobile
- [ ] Social proof and stats bar are full-bleed
- [ ] Effects page has left sidebar with categories, filters, and effect grid
- [ ] Platform page has left sidebar with tier groups and product grid
- [ ] Docs pages have sidebar + table-of-contents
- [ ] No `overflow: hidden` on header or body (except during modals)
- [ ] All touch targets ≥ 44×44px at 375px
- [ ] Theme toggle works — dark/light with proper `[data-theme]` switching
- [ ] No concatenated words in headings
- [ ] All meta tags, canonical URLs, OG tags correct
- [ ] No ghost dialogs persisting after dismissal
- [ ] All modals have properly spaced aria-labels
- [ ] Lighthouse: Performance >90, Accessibility >95, SEO >95
- [ ] Zero features, effects, or products lost during rebuild
- [ ] Page height is no longer 35,000px (distributed across routes)

---

That's the complete rebuild prompt. It incorporates **every bug** found across 5 QA audits, the **exact redesign specification** from your screenshots (navbar, hero split, Roy Studio panel, feature grid, social proof, stats bar), **multi-page architecture**, **responsive breakpoints**, and **8 parallel agent work streams** with clear acceptance criteria.